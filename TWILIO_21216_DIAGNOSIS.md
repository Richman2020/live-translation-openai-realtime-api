# Twilio 21216：历史通话与账户限制排查

检查时间：2026-09-26，用户本机真实 Twilio API、已登录的 Trust Hub 网页及工作台通话事件。号码、账户 SID、地址、身份资料、密钥及完整支持材料不进入共享仓库。

## 最新结果（2026-09-26）：企业审核通过，目标外呼已接通；故障转为通话链路问题

- 官方页面当前显示 Primary Business / **Approved**；按当前页面档案标识精确读取 API，同一账户返回 `twilio-approved`。当前档案标识已变化，旧档案精确读取为 404。旧版列表遗漏和旧档案待审记录都不能用来判断当前状态。
- 关联法定地址仍为国家 `CN`、`validated: false`，与已核对的法定地址不一致。不能由 Approved 推断全部资料正确；地址问题仍待更正。本轮尚未向支持发送消息，旧待审草稿须按当前获批事实更新。
- 本轮发现原本机服务已停止、旧临时隧道失效；已恢复正式服务和新临时隧道，真实供应商检查 **4/4 通过**，原号码与原 TwiML App 的回调改绑后经远端回读确认。公开入口仍为临时隧道，没有重启自动恢复及自动改绑。
- 对已授权目标进行受控直接 PSTN 对照，Twilio 接受请求，依次为 `queued` → `ringing` → 20 秒后的 `no-answer`，没有返回 `21216`。这说明原拒绝没有阻止该次请求；未接听的这次对照不验证声音或翻译。
- 用户确认可以接听后，工作台真实通话于 `2026-09-26T01:46:19Z` 接通，SSE 接通窗口约 58 秒。Twilio 最终浏览器线路 65 秒、PSTN 线路 59 秒，均为 `completed`。两个媒体流都有 `started` → `stopped`，没有流错误，状态回调为 204、TwiML 为 200；不能推断我方媒体流未建立。
- 实际 SSE 收到对方（`remote`）原文 4 条及译文 4 条，没有我方（`local`）字幕。原始失败事件为 `openai_connection_closed:remote`，即对方对应的 OpenAI WebSocket 关闭直接触发应用结束，具体关闭原因仍在排查。UI 的 `31005` 是浏览器信令错误，不能替代原始失败证据。旧会话结束后幂等挂断回读确认无待清理线路、`activeSession: null`。
- **第二通修复后实测**：Twilio 浏览器线路 146 秒、PSTN 线路 127 秒，于 `2026-09-26T02:36:02Z` 均 completed；双媒体流 started → stopped，无流错误或 Notifications，回调 204。后端正常 completed、error 为 null、无待清理线路，期间没有 OpenAI 关闭；UI 收到对方原文 11 条、译文 11 条，我方为 0。SDK 有 low-bytes-sent、高丢包、恒定输入、低 MOS 警告，不能称双向音频或网络质量验收。
- 第二通挂断后原默认耳机麦克风 20 秒检测 RMS 为 0、无有声帧；随后当前 Windows/浏览器可用设备换为 USB 摄像头麦克风，指定设备 20 秒检测峰值 RMS 0.11337，1202 帧中 756 帧有声。刷新工作台并重新获取当前默认麦克风后完成第三通，已出现我方字幕。
- **第三通真实结果**：浏览器线路于 `2026-09-26T02:43:22Z` 开始、150 秒，PSTN 线路于 `02:43:42Z` 开始、130 秒，均 completed，按时间和时长计算结束于 `02:45:52Z`；`02:47:56Z` 只读检查 `activeSession: null`。完整 UI 有我方原文 25 条、译文 25 条，对方原文 12 条、译文 12 条；两个 OpenAI 都 ready 且未再关闭。浏览器发送约 8000 字节/秒、接收 6880–8000 字节/秒，输入输出音量均曾非零，采样丢包 0–14%。已取得双向输入和字幕的真实证据，用户耳听反馈尚未确认，不能称音质、准确度或稳定性通过。
- 第三通发现音响回录、转写差异、译文答非所问的候选问题；已用共用函数统一 session/每次 response 的翻译提示，加入保留问句、指代、否定及数字的例子。这是尚未真实质量复验的候选修复。UI 已按 `role + itemId + contentIndex` 逐句配对原文/译文，实时单行插入替换，历史记录与导出一致，陈旧待清理提示也已修复。最终代码检查通过，最终版本已安全加载并回读注册成功；等待用户声音反馈后做固定句耳机隔离对照，不继续自动拨号，不能把字幕数量或正常结束写成全部完成。

### 同日恢复修复及根因证据边界

- 已就绪的 OpenAI 连接遇到允许的传输/服务关闭，每个方向最多重建一次，握手期限 10 秒；政策、认证、格式或配置错误不重试，第二次断开结束并清理。恢复只保留最近 2 秒待发输入，不重放旧 turn、item ID 或生成语音；旧连接事件与延迟写入回调已隔离，`CLOSING` 先于 `close` 的竞态已修复。
- UI 分别显示翻译恢复、提示重说和最长 30 秒 Twilio 信令恢复窗口，隔离旧会话事件；诊断日志脱敏。独立审查验证关闭窗口、双方向并发恢复和延迟回调边界。SDK volume 声音检测、独立网络质量提示、每 5 秒纯数值 RTC 诊断也已实现。包含最终提示词、字幕配对及清理提示修改的全量 `npm test` **92/92 通过**，构建、前端语法、定向 ESLint、差异检查均通过；此前 86 项全量/23 项定向是已被本次验证覆盖的中间记录，不代表真实质量验收。
- 两组真实 OpenAI 静音连接有/无客户端 ping 均正常维持约 88 秒，已有服务端 ping，未复现第一通断开。此结果不能把底层原因确定为固定超时或 ping 缺失。
- 真实 bridge 故障注入中，两端真实 OpenAI ready 后强制断开对方连接（1006），约 1.716 秒后新会话 ready，内存电话始终 OPEN，最终全部 3 个 OpenAI WebSocket 均确认清理。测试不含真人音频、`response.create` 或 Twilio 电话，仅验证真实 OpenAI 重连能力，不能称真人电话恢复或翻译验收。
- `Stop-AIPhone.ps1` 已取得清理与安全停止确认，随后 `Start-AIPhone.ps1` 加载受控恢复版本。第二、三通结果已单独记录；第三通后的提示词和字幕配对修改已通过最终代码验证，最终版本已安全加载，页面回读为「电话已开启」、拨号按钮可用。没有把代码、服务运行或模型连接恢复等同于完整通话效果。

## 2026-09-24 历史结果：企业主档案已提交待审，地址国家不一致已确认

- Trust Hub 页面显示现有 Business Primary Profile 为 Rejected，错误 `18602`：`The Business ID you provided could not be verified`。旧表单含个人名称、Sole Proprietorship、Other 注册类型及旧地址/网站；企业编号无法核验是页面明确给出的档案拒绝原因，但不能据此保证它是全部外呼限制的唯一原因。
- 已进入该档案 Edit and resubmit，依据用户提供的真实文件更正法定名称、EIN 注册类型和编号、LLC 类型及网站，核对代表/联系人并更新职务为 Sole Member / Other，通知邮箱保留现有账户联系人邮箱。
- 表单两处要求填写与企业注册编号关联的法定地址，已使用企业税号信与注册文件中一致的法定/邮寄地址；没有声明其为美国实际办公室。用户确认的真实经营地址仍单独保存在本机私密资料中，本次未出现实际经营/办公地址问题。
- Persona 起始页的生物识别处理及最长三年保留提示已取得用户针对该提示的确认后继续；本次没有要求自拍或上传证件，不能据此称已完成生物识别验证。
- 提交及 Finish 后主档案页面显示 Business / **Pending review**，详情页显示 **In review**。按页面标识精确 API 读取返回 `pending-review`，且账户与本机配置及网页账户一致。法定名称、企业编号、LLC 类型、网站和代表字段回读正确，但表单选择的法定地址国家未保留：详情页与关联地址 API 均显示国家不一致，API 返回 `validated: false`，城市与所填法定地址一致。仅能确认提交成功，不能称全部字段正确；企业审核尚未通过，本次未再次拨号，`21216` 是否解除仍未验证。
- 在审页面没有编辑入口；[官方主档案说明](https://www.twilio.com/docs/trust-hub/trusthub-rest-api/console-create-a-primary-customer-profile)也不允许编辑审核中的档案。需准备官方支持更正请求，取得用户发送授权后提交，不直接改写审核资源绕过流程。
- 浏览器已能完成此流程，但操作约 21 秒、部分读取约 42 秒。当前 Codex/CUA 进程缺少代理环境，独立代理探针成功不等于当前进程已稳定恢复；该连接问题与 Twilio 服务端拒绝分别处理。

## 提交前已核实的结果（历史 API 与通话快照）

1. 当前 API 对应用户原有客服项目的活动 Full 主账户；该账户返回的 `ownerAccountSid` 与自身一致，可见账户列表仅返回该账户。号码、电话应用及获批主档案均属于该账户，未发现本应用调用另一个子账户的证据。此结果不代表用户登录身份下不存在其他独立账户。
2. 原号码确实有多次历史 `inbound` / `completed` 记录，部分持续 130–211 秒。检索到的 11 条从原号码发出的 `outbound-dial` 记录均为 `failed`、0 秒。因此“此前接通过电话”有呼入记录支持，但不能据此认定此前已成功向手机外呼。最近三条历史失败通话的通知接口均返回空列表，不能把历史失败原因也认定为 21216。
3. 原号码私密备份显示，旧语音入口是外部自定义应用的 POST 回调，并非直接绑定 Studio。改回旧入站回调没有证据能解除本次外呼限制。
4. 2026-09-24T03:42:19.332Z 的最小外呼对照使用同一项目 API Key、Account SID、原号码和用户已授权的唯一测试目标，提供内联 TwiML，未使用 OpenAI 或本机回调。Twilio 直接返回 HTTP 400、21216，完整消息为 `Account not allowed to call [approved test destination]`；目标 Call SID 未创建。该结果把此拒绝与翻译、麦克风及本机回调处理区分开来。
5. 美国普通号码与特殊号码拨号权限已开，高风险欺诈号段权限未开。没有为了尝试通话而扩大高风险权限。
6. 旧版列表接口返回同账户的获批 Individual Primary Profile，未返回 Business Primary Profile。后续网页已显示被拒企业档案，提交后按网页标识精确读取亦返回该同账户企业档案，而旧版列表仍未列出它。因此这是列表与精确读取的可见性差异，不能仅归因于时间变化，也不能继续表述为当前没有企业档案。此前沿个人主档案的实体关联读取地址，发现档案国别与用户描述的美国账户存在差异；具体字段仅在本机私密支持材料保存。公开 API 未直接给出开户地区分类，原始外呼响应也未标明具体规则，不能把当时推断写成最终供应商结论。
7. 近七天 Monitor Alerts 返回空列表；此结果不否定已收到的 REST 400 响应。供应商原始响应没有更细的拒绝字段，本次未捕获请求标识，不伪造目标 Call SID 或请求 ID。

## 21216 的官方处理路径与历史支持准备

根据 [Twilio 21216 说明](https://www.twilio.com/docs/api/errors/21216)和[美加外呼认证要求](https://help.twilio.com/articles/42720101060379)：

- 如果 Twilio 将账户归类为 2025-10-08 起在美加以外创建，需提交真实的 Business Primary Compliance Profile 并通过审核。个人主档案获批并不替代该条件；官方表示支持团队不能手动豁免这一要求。
- 如果已登记的地址或开户地区分类有误，应先根据真实资料请 Twilio 核实更正。不能仅因为使用美国电话号码或 US1 区域就认定账户为美国主体，也不能为了拨号填写虚假美国地址或企业资料。
- 如果 Twilio 确认账户已满足适用的主档案要求，则继续由支持团队核查目的号码风险或其他限制。请求其明确账户分类、具体拒绝规则以及保留原号码的整改路径。

2026-09-24 的最小外呼支持草稿和待审档案地址更正草稿保留为历史材料，位于受限 ACL、Git 忽略的 `.runtime/support-materials/business-profile/`；其中包含确切档案标识、法定地址及其用途说明，不含密钥、密码或完整税号，也未附证件。当时找到同类已关闭工单，先前支持曾要求具体档案 SID；补充说明曾填入评论框但未发送。该历史草稿的档案状态和标识已过时，不能直接发送。

当前企业审核已通过，同一已授权目标已有外呼接通证据，不能继续将 21216 表述为当前实测阻塞。保留地址国家差异这一独立资料问题，按真实资料和官方可用流程请求更正；截至本次记录尚未发送支持消息，不因通话放行而忽略错误字段，也不以绕过审核或重新购买号码处理。若后续请求再出现 21216，按当次错误核对剩余账户或目标限制，不以连续重复拨号代替诊断。

## 验收边界

供应商当前审核状态由官方页面和同账户精确 API 确认；真实手机外呼接通、第三通双向原文/译文和正常结束已有实际证据。两个方向用户实际听到的翻译音频、字幕准确度、真实来电、稳定性、质量、端到端延迟及费用仍未完整验收，不以代码测试、API 通过或双向字幕数量替代。2026-09-26 已进行获授权的真实电话测试，未新建密钥/号码，未上传证件/自拍，也未发送支持消息；具体应用修复和验证进度以 [PROGRESS.md](PROGRESS.md) 最新记录为准。
