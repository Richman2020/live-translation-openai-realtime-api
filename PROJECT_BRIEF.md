# 中英双向电话翻译：项目目标与范围

共享仓库：<https://github.com/Richman2020/live-translation-openai-realtime-api>

本文件保存用户已表达的需求与当前实现范围；实际开发进度见 [PROGRESS.md](PROGRESS.md)，协作规则见 [AGENTS.md](AGENTS.md)。需求变动应同步修改本文件。

## 用户要完成的事情

用户希望用美国虚拟电话号码与美国人直接沟通：用户说中文，对方听到英语；对方说英语，用户听到中文。可同时显示文字，必要时接受先用文字帮助理解。核心关注是真实通话中的延迟，不能只展示界面或用模型内部耗时证明“已经低延迟”。

用户期望最终从桌面入口直接启动使用，并希望以后可以接入 WhatsApp 或其他通话渠道。桌面入口、打通真实电话和接入其他平台是不同的工作项，应分别验证。

用户也关注持续使用成本，希望了解翻译 API 与电话线路的费用，并探索更便宜的模型或服务。此前提到 OpenAI、DeepSeek、Palabra 和 Tranlico；这些是待比较的方案，没有据此决定更换当前实现，也没有已完成的效果或成本对比测试。

## 当前技术起点

代码来自 [Twilio 官方示例](https://github.com/twilio-samples/live-translation-openai-realtime-api)，是 Node.js/TypeScript 后端中间服务，连接 Twilio Voice、Studio、Flex、TaskRouter 与 OpenAI Realtime。

原版流程为：来电者拨入 Twilio 号码并选择语言，Flex 坐席接听，两条语音流分别翻译后送到对方。来电语言可选普通话，原版坐席语言为英语。

`src/routes/outbound-call.ts` 是 Flex 号码的回调处理，并不等于已经实现“在桌面输入美国客户号码主动外呼”。把用户作为说中文的坐席并主动呼叫英语客户，还需要实现和验证相应通话流程。

## 分阶段完成范围

1. **建立可复现的开发基础。** 安装依赖，修复编译与 Realtime 接口兼容性，提供配置检查和离线验证，让电脑端 Codex 可以从本仓库接手。
2. **验证现有示例的真实双向翻译。** 配好本地秘密变量、Twilio 与 ngrok，先跑通来电到 Flex 的流程，确认两个方向实际听到正确语言，并记录延迟。
3. **满足用户的直接通话场景。** 在验证基础上实现中文使用者的操作入口、号码输入、主动外呼、接听/挂断及状态提示，确认语言方向与用户身份相符。现有桌面端工作应先同步并审查，再决定需要补写哪些部分。
4. **改善使用体验。** 增加可读字幕与必要的错误提示，优化实际测得的延迟，评估可运行的桌面交付方式。

这是当前工作顺序，不能把计划项写成已实现。用户尚未指定硬性毫秒验收阈值；先实测，再据结果确认可接受范围和优化目标。

## 延迟与效果的验证口径

- 分别测量中文到英文、英文到中文，从说话结束到对方实际听到翻译的时间；条件允许时保留各阶段时间戳，以定位网络、识别/模型与播放耗时。
- 使用短句、长句、数字/姓名、连续对话和打断等实际场景，检查漏译、错译、串音、回声和挂断后的音频行为。
- 当前提示词仍要求等说话者完成一轮后翻译。不能将其描述为已经实现逐词同传，也不能承诺尚未测得的“亚秒延迟”。
- 代码中的语音结束事件到首个生成音频片段的时间只是局部指标，不等于对方实际听见的端到端延迟。
- 成本对比应使用同一场景、相同通话时长和实际用量记录；没有数据时不宣称哪家更便宜或效果更好。

## 尚未完成或尚未选定

- 本仓库尚未验证独立桌面界面、主动外呼或字幕。用户报告在电脑端 Codex 创建了桌面快捷方式，其代码和验证结果需要从电脑端同步后确认。
- WhatsApp 及其他渠道接入属于后续需求，当前没有已完成的实现或接入验证。
- 使用 DeepSeek 等其他模型的成本优化属于研究方向。不能只替换模型名称就宣称已有实时语音能力；需要分别评估该方案需要的识别、翻译、语音输出和延迟。
- 持续部署地点、桌面封装方式、最终供应商组合及计费方案尚未确定。
