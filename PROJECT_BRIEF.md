# 中英双向电话翻译：项目目标与范围

共享仓库：<https://github.com/Richman2020/live-translation-openai-realtime-api>。更新日期：2026-09-26。

本文件保存用户需求、决定与实现范围；验证进度见 [PROGRESS.md](PROGRESS.md)，运行步骤见 [LOCAL_SETUP.md](LOCAL_SETUP.md)，协作规则见 [AGENTS.md](AGENTS.md)。

**2026-09-26 当前进度：Twilio 企业主档案已获批，第三通正常结束并产生双向原文/译文；用户实际听感和翻译质量尚未验收。** 官方 Primary Business 页面为 Approved，当前档案精确 API 返回同账户 `twilio-approved`，旧档案标识已失效；关联法定地址国家仍不一致（`CN`、`validated: false`），尚未发送支持更正请求，不能称资料全部正确。本机服务和临时隧道已恢复，真实供应商验证 4/4 通过，原号码与原 TwiML App 回调改绑及远端核验完成，目标外呼不再返回原 `21216`。第一通发生 OpenAI 连接关闭，第二通正常结束但无我方字幕；第三通浏览器 150 秒/PSTN 130 秒均 completed，按开始时间和时长计算结束于 `02:45:52Z`，`02:47:56Z` 只读检查无活动会话。UI 有我方原文 25 条/译文 25 条、对方原文 12 条/译文 12 条，两个 OpenAI 均 ready 且未再次关闭；发送约 8000 字节/秒、接收 6880–8000 字节/秒，输入输出音量均曾非零，但采样丢包为 0–14%。这些是实际链路和字幕证据，不能称用户耳听、准确度或稳定性验收通过。

第二通结束后原默认耳机麦克风 20 秒检测 RMS 为 0、没有有声帧；当前 Windows/浏览器可用设备换为 USB 摄像头麦克风后，指定设备 20 秒检测 RMS 峰值 0.11337，1202 帧中 756 帧有声。工作台刷新并重新获取默认麦克风后，第三通已出现我方原文和译文。仍有音响回录、转写差异及译文答非所问的候选问题，需固定句、耳机隔离对照。已用共用函数统一 session 与每次 response 的翻译提示，加入保留问句、指代、否定和数字的例子；这仍是未做真实质量复验的候选修复。原文/译文已按 `role + itemId + contentIndex` 逐句配对，实时单行插入替换、历史与导出一致，陈旧待清理提示也已修复。代码验证通过，最终版本已安全加载，页面回读为「电话已开启」、拨号按钮可用；用户尚未回复耳听反馈，不把双向字幕等同于双向电话效果验收。

**同日修复进度**：后端已支持就绪 OpenAI 会话按方向各一次受控恢复，10 秒握手期限、2 秒待发输入上限，不重放旧语音或 turn；政策、认证、格式错误及第二次失败仍结束清理。UI 分别提示翻译恢复和重说、Twilio 信令最长 30 秒恢复，隔离旧会话事件并记录脱敏诊断；`CLOSING` 与 `close` 的竞态已修。SDK volume 声音检测、独立网络质量提示、每 5 秒纯数值 RTC 诊断也已实现。包含最后提示词和字幕配对修改的全量 `npm test` **92/92 通过**，构建、前端语法、定向 ESLint 与差异检查均通过，覆盖此前 86 项全量/23 项定向的中间结果。两组真实 OpenAI 静音连接（有/无客户端 ping）均正常约 88 秒，未复现原断开，不能认定固定超时或 ping 是根因。真实 OpenAI 故障注入后替代会话约 1.716 秒 ready、内存电话保持 OPEN，3 个连接均已清理；没有真人音频、生成响应或 Twilio 电话，不替代真电话验收。已按正式安全停止/启动流程加载的恢复版本完成第二、三通实测，最终版本亦已安全加载并回读注册成功。

2026-09-24 历史记录：用户说明账户是美国账户，要求继续使用此前注册的 Twilio 号码。当日核对原有号码的归属、语音能力、运行配置和回调均正确；当时实测仍返回 21216/HTTP 400，目标手机通话未创建。未将企业档案缺失直接定为唯一原因；该拒绝记录不能覆盖 2026-09-26 已接受并接通的最新结果，见 [PROGRESS.md](PROGRESS.md)。

同日后续历史记录：真实 Trust Hub 页面显示已有 Business Primary Profile 被拒，错误 `18602` 指企业编号无法核验。已通过 Edit and resubmit 更正法定名称、EIN、LLC 类型、网站及代表信息并提交待审；上述字段回读正确，但提交后地址国家未保留所选值，地址 API 为 validated: false。旧版列表仍只返回个人主档案，按网页标识精确读取则返回同账户企业档案，证明列表不完整，不能据此认定企业档案不存在。浏览器当时已能完成操作，但仍有明显延迟及代理继承问题，不能称控制连接已稳定修复。详细证据及当前获批结果见 [TWILIO_21216_DIAGNOSIS.md](TWILIO_21216_DIAGNOSIS.md)。

## 用户要完成的事情

用户希望通过美国虚拟电话号码直接与美国人沟通：用户说中文，对方听英语；对方说英语，用户听中文，并可查看字幕。用户希望点击桌面入口即可操作，既能输入号码主动呼叫，也能接听来电。

核心关注是真实通话的翻译质量和端到端延迟。界面展示、配置格式检查或模型内部耗时都不能证明整条链路已经接通或低延迟。未来希望研究 WhatsApp 等渠道及更低成本的方案；OpenAI、DeepSeek、Palabra 和 Tranlico 是被提及的候选，尚无共同场景下的效果或成本对比结果。

## 已确定的运行与号码决定

- 现阶段在用户电脑上的 Codex 开发和运行，暂不部署 Railway 等云端服务；密钥只留在本机私密运行环境。
- 用户已批准复用现有 Twilio 号码，并用本翻译系统替换旧系统语音接入，无需重复索取是否保留旧语音回调的授权。先保证新服务、公开入口及 API 验证有效，再改绑。
- 此项授权不包括删除旧系统数据或其他资源，也不代表开通新的 Flex 套餐或购买新号码。
- 用户要求完成 OpenAI 接入和真实双向电话闭环；供应商接入已验证，真实电话闭环仍是待完成的验收目标。
- 用户已授权使用公司文件办理 Twilio 企业认证，并确认真实经营地址。此次实际表单要求与注册编号关联的法定地址，已按企业税号信与注册文件填写；真实经营地址单独保留，不把法定/邮寄地址声明为美国实际办公室。Persona 的具体生物识别处理及保留提示已取得用户确认，本次没有自拍或文件上传步骤。身份字段只保存在本机私密材料。

## 当前代码实现：独立 solo 单人模式

仓库在上游 Twilio Flex 示例之上新增 `src/solo/` 与 `public/`，本轮已实现浏览器电话与本机服务，作为当前桌面入口的运行模式。采用 Twilio Voice JavaScript SDK、Twilio Voice/Media Streams 与 OpenAI Realtime；本模式不依赖 Flex、Studio 或 TaskRouter，保留原版实现供独立使用。

| 范围 | 当前实现 | 验收边界 |
| --- | --- | --- |
| 桌面入口 | Windows 启动/停止/快捷方式脚本；复用既有中文预览视觉 | 已有本机启动与隔离界面验证，具体记录见进度 |
| 通话操作 | 单通话主动拨号、接听/拒接、挂断、静音、忙线拦截；麦克风先预检，30 秒超时/取消、旧连接隔离；安全错误码、21216 指引与最长 30 秒浏览器信令恢复 | 第二、三通 PSTN 127/130 秒正常结束；第三通采样丢包 0–14%，稳定性、来电及实际听感待验收 |
| 双向翻译 | 我方普通话→英语、对方英语→普通话；双流配对，默认 `gpt-realtime-1.5`，共用可选代理；已就绪连接按方向各一次受控恢复并提示重说 | 真实 Realtime 会话和故障注入恢复已验证；后者未接真实电话，不证明音质、翻译效果或端到端延迟 |
| 字幕与记录 | 实际事件驱动原文/译文，按 role + itemId + contentIndex 逐句配对、单行更新；可选浏览器本机历史与导出一致，默认不保存 | 第三通我方原文/译文各 25 条、对方各 12 条；配对修复已通过代码测试，数量不证明准确，真实质量对照待完成 |
| 配置与准备 | 7 项供应商设置、资源准备、4 项真实 API 检查、号码改绑及远端回读均已完成 | 配置、API 及注册成功不等于真实电话验收；临时隧道变化后须重新准备/验证/改绑 |
| 线路结束 | 挂断清理；未确认时保留会话、允许重试并阻止新拨号；停止服务先取得安全确认 | 第一通失败清理、第二三通正常结束均已确认；第三通结束后无活动会话，完整效果与费用仍待验收 |

页面不生成模拟对话，不把“配置齐全”显示为通话验收成功。浏览器只有用户开启通话后才注册设备，拨号或接听时预检麦克风；外呼仅在准备成功后创建后端会话，预备流单次交给 SDK。缺本机访问令牌的错误现全局可见，设置页也提供重新从桌面入口打开的指引。控制 API 只允许本机鉴权访问；公开路由限签名校验的语音入口及最小健康探针。

当前已取得恢复运行后的正式 `verify:providers` 4/4、原号码与 TwiML App 回调改绑回读，以及真实手机接通和双向原文/译文证据；第一通断线、第二三通正常结束，第三通更换当前麦克风后已出现我方字幕。实际听感和翻译质量仍未验收。此前两条仅持续 2 秒和 1 秒、目标手机通话未创建并返回 21216/HTTP 400 的记录保留为历史。麦克风授权后旧请求曾继续等待，无活动电话时刷新工作台并重新注册后预检成功。新增安全供应商错误码及前端指引时，80/80 项测试、构建、前端语法、TypeScript、定向 lint 与差异检查通过；这些历史代码验证及原 76 项、未授权超时/取消证据均不替代当前真实电话结果，最新验证范围见本页顶部。

2026-09-22 的“供应商运行凭据未配置、尚无真实 OpenAI/Twilio API 结果”保留为历史快照，详见 [PROGRESS.md](PROGRESS.md)；不可再作为本机当前状态。真实设置只在忽略的 `.env`，原号码语音备份只在私密 `.runtime/`；共享文档不保存 SID、号码、密钥、临时域名或私密账户资料。

## 下一阶段：验收真实电话链路

1. 安全加载并核验最终版本，完成提交推送；先等待用户实际声音反馈，不继续自动拨号。随后用固定句、耳机隔离对照核验听感、转写、翻译及延迟，并结合数值 RTC 诊断处理网络丢包。第一通 `openai_connection_closed:remote` 的具体原因尚未复现。另处理已获批 Business Primary Profile 的地址国家差异；当前 Approved 不等于全部资料正确，旧待审草稿须更新后使用，不重建已通过验证的密钥或号码，详见 [PROGRESS.md](PROGRESS.md)。
2. 保持本机服务和公开 HTTPS/WSS 隧道运行。临时地址变化后更新 TwiML App 并重新验证，当前没有隧道自动恢复及自动改绑。
3. 仅在公开地址或相关配置变化后，重新运行 `configure:twilio -- --apply` 更新号码回调并回读核验；SMS 配置保持不变。
4. 企业审核和该目标外呼接通已有真实证据；修复后继续向用户已授权测试目标受控复测，确认双方实际听到翻译，再验证来电、中英两个方向、字幕、拒接/忙线/掉线及任一侧挂断，记录延迟和费用。若再次被拒，按当次供应商错误重新核对，不沿用旧拒绝结论。
5. 根据真实结果优化体验，再决定额外渠道、模型或桌面封装；当前快捷方式打开浏览器应用，并非 Electron 原生安装包。

## 延迟与效果的验证口径

- 分别测量中文到英语、英语到中文，从说话结束到对方实际听到翻译；有条件时记录识别、模型、网络和播放各阶段时间。
- 测试短句、长句、数字/姓名、连续对话和打断，检查漏译、错译、串音、回声以及挂断后的音频行为。
- 当前翻译流程按说话轮次工作，不能称为已经实现逐词同传；用户没有指定硬性毫秒阈值，不承诺尚未测得的亚秒延迟。
- 模型首个生成音频片段耗时只是局部指标，不能代替对端实际听见的端到端结果。
- 成本比较应使用同一场景、通话时长和实际用量，包含电话线路与模型用量；没有数据时不宣称哪家更便宜或效果更好。

## 尚未实现或尚未验证

2026-09-22 完整代码复核还确认：字幕断线补发、通话中 DTMF 按键菜单、完整延迟/用量记录与 Twilio 自动回滚命令尚未实现；桌面入口只启动本机服务，隧道和变化后的号码回调仍须手动恢复。详细能力与验收边界见 [READINESS_REVIEW.md](READINESS_REVIEW.md)，不将这些缺项归为“只差 API 密钥”。

WhatsApp/其他渠道、其他翻译供应商、生产云端部署、自动隧道域名恢复以及真实电话效果/费用验收均未完成。Twilio/OpenAI 仍是在线服务，电脑端运行不等于离线或免费。

代码来源为 [Twilio 官方示例](https://github.com/twilio-samples/live-translation-openai-realtime-api)。原版 Flex 的 Studio、TaskRouter、坐席与双号码说明保留在 README 和安装文档单独章节，不作为 solo 运行前提。
