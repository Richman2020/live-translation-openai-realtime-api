# 项目完整复核：已实现与剩余工作

复核更新：2026-09-24（用户 Windows 电脑）。共享仓库：<https://github.com/Richman2020/live-translation-openai-realtime-api>。

**当前结论：7 项供应商设置、正式 API 验证 4/4、号码改绑回读、浏览器注册及麦克风预检均通过；当前目标手机外呼被 Twilio 21216/HTTP 400 拒绝。** 两次真实浏览器通话已形成，但手机接通、来电、双向翻译音频及延迟尚未验收。不能将浏览器线路成功称为完整电话验收。第 4 节的体验和恢复能力缺项仍存在。

## 2026-09-24 最新复测

用户说明账户为美国账户；原有号码的账户归属、语音能力、运行配置及两个语音回调均已实时核对正确。再次真实拨测仍返回 21216/HTTP 400，浏览器线路持续 2 秒后结束，目标手机通话未创建，清理已确认。未更换号码，具体供应商拦截规则仍未确定。本轮仅更新文档，未重复执行应用代码测试；详细证据见 [PROGRESS.md](PROGRESS.md)。

## 2026-09-23 接入与验证记录

| 检查对象 | 本轮实际结果 | 证据边界 |
| --- | --- | --- |
| 网页控制 | Codex 内置浏览器完成 Twilio/OpenAI 操作、注册和麦克风预检/超时/取消真机验证 | Chrome 本轮约 21 秒后 fetch 失败，独立诊断中 |
| 7 项供应商配置 | 6 项 Twilio 设置及通过用户明确批准网页创建的 OpenAI 密钥均已保存；`check:solo` 的 10 项必需设置全部 `ready` | 只保存在受限且忽略提交的 `.env`；不记录 SID、号码、秘密值或账户资料 |
| 本机与公开入口 | 本轮 TwiML App Voice 地址/方法匹配，公网健康探针 200 且应用标识正确，无签名 `/voice/client` 返回 403 | 临时地址不入库；拒绝无签名请求不等于真实电话签名/媒体流验收 |
| Twilio API | `twilioAccount`、`twilioNumber`、`twilioApplication` 全部通过，为 `VERIFIED_RESOURCE` | 正式 `verify:providers` 时间为 `2026-09-23T06:05:55.183Z`；该检查不拨号 |
| OpenAI | 同次正式验证取得 `SESSION_UPDATED`；可选 `OPENAI_PROXY_URL` 由验证与实际翻译连接共用 | 已验证真实 Realtime 会话，不发送音频，未验收翻译听感；代理仅在本机私密保存 |
| 号码路由与电话 | `--apply` 返回 `status: configured`，现有号码 Voice 回调已改绑并回读核验；SMS 未改，原语音备份在私密 `.runtime/` | `realCallTested: false` 指改绑工具本身不拨号；随后 UI 拨号尝试结果单列 |
| 浏览器线路注册 | 点击“开启通话”后显示“电话已开启”“已注册 · 可接收来电”，拨打按钮可用 | 注册不等于电话接通 |
| 首轮 UI 拨号（历史） | 未授权阶段等待 75 秒后 `CALL_SETUP_TIMEOUT`，当时 Twilio Calls 为空 | 该阶段没有到达真实电话线路；不能作为当前仍无 Twilio 通话的证据 |
| 麦克风独立诊断 | 用户授权后取得 1 条有效音轨并立即释放；工作台刷新、重新注册后真实预检成功 | 不录制或上传音频；旧请求可能在权限为 `granted` 后仍等待，只在无活动电话时刷新 |
| 超时与取消（历史验证） | 未授权时约 30 秒显示 `MICROPHONE_TIMEOUT` 并恢复，取消准备也立即恢复；后端会话为空 | 验证准备未完成时不创建电话；当前已实际通过授权，不再以等待权限为阻塞 |
| 授权后两次真实拨号 | Twilio 浏览器通话分别持续 2 秒和 1 秒，目标手机通话均未创建；第二次明确 `21216` / HTTP 400 | 已到真实浏览器线路，手机响铃/接通、来电与双向翻译音频仍未验收 |
| 外呼拒绝排查 | 账户 Full/active、余额为正、美国普通号码地理权限开启；已有获批 Individual Primary、缺 Business Primary，账户创建日期在 2025-10-08 及之后 | 用户已说明为美国账户；上述只读结果不能单独证明 21216 的具体根因 |
| 清理与运行 | 失败后清理确认、`activeSession: null`；正式停止脚本确认安全后已重启服务 | 此次失败线路清理不替代正常完整电话挂断及费用验收 |
| 前端与诊断修复 | 先预检再建会话、单次复用媒体流、30 秒超时、取消/延迟结果/旧 Device 隔离；新增合法数字错误码、HTTP 状态及 21216 指引 | 不暴露原始供应商错误；页面提示 Trust Hub/号码限制，未断言 Business Profile 是根因 |
| 构建与检查 | 本轮最终 80/80 项测试、构建、前端语法、TypeScript、定向 lint 与差异检查通过 | 全内存探针仍使用供应商替身；离线回归不替代真实电话与翻译 |
| 共享版本 | `codex/local-phone-workbench` 的 `886b62a` 已核对远端 | [草稿 PR #2](https://github.com/Richman2020/live-translation-openai-realtime-api/pull/2) 未合入；本次新增代码与文档推送以 Git 核验为准 |

当前顺序：按用户已说明的美国账户及 [Twilio 官方 21216 说明](https://www.twilio.com/docs/api/errors/21216)核对风险、监管与适用的 +1 Primary Profile 条件；处理供应商限制后，再验证目标手机外呼、来电、两个翻译方向、字幕、挂断与延迟。麦克风授权及浏览器线路已通过，不重复创建密钥或资源，不用连续重拨代替限制核验。临时隧道重启换址后仍须更新 App、验证 API、重新改绑并回读核验，尚无自动恢复机制。

## 2026-09-22 完整代码复核历史快照

下方第 1–3 节保留 2026-09-22 的证据与当时执行计划，包括“7 项 missing”“没有创建供应商资源”“尚无 OpenAI 会话或号码改绑”等旧状态；当前接入及浏览器注册结果以上方 2026-09-23 为准。第 4–5 节的实现限制与正式验收口径仍适用。

**2026-09-22 当时结论：独立 solo 工作台及核心通话代码已实现，但尚不能真实使用；不能说只差两个 API 密钥、其余已经全部完成。** 当时供应商配置、资源准备、号码改绑与真实通话验收均未完成，另有明确的体验和恢复能力缺项。

## 1. 本机与 GitHub 为什么此前结论不同

- 此前本机 `codex/local-phone-workbench` 包含实现提交 `d9394f4` 和说明提交 `5c1c8ac`，因网络失败没有推送。GitHub 主分支未包含 `src/solo/`、`public/` 和桌面启动器。
- 本次重新连接 GitHub 成功。远端 `main` 为 `5fabf51cd6976131ac829b1b95c87f7239096184`，新增的是网页版 Work 对旧代码的复核记录，没有新增 solo 实现。
- 本轮安全整合该远端记录，保留两边历史，明确 Work 旧环境和用户电脑 solo 环境的区别。没有重置或覆盖网页版提交。
- 完整代码、检查记录和剩余清单已推送到 `codex/local-phone-workbench`，交付提交 `457ea22f99e7af3da3efe3777dd17dc7b84b8b7d` 已通过远端分支 SHA 和 GitHub 文件接口核对。已建立[草稿 PR #2](https://github.com/Richman2020/live-translation-openai-realtime-api/pull/2)，尚未合入 `main`；本记录后续维护提交以该分支最新提交为准。
- 网页版后续应显式读取该开发分支及本文件。若仅读取尚未合并的 `main`，仍会看到旧 Flex 版本。GitHub 代码同步也不代表网页版能读取本机密钥或自动接通电话。

## 2. 2026-09-22 能力与证据快照

| 项目 | 代码/实际检查 | 仍缺什么 |
| --- | --- | --- |
| 中文桌面工作台 | 已实现；桌面快捷方式指向本仓库启动脚本 | 当前为浏览器应用窗口，不是原生安装包 |
| 主动拨号、来电接听/拒接、静音、挂断 | solo 状态机、Voice SDK 与路由代码存在；离线测试通过 | 真实电话逐项验收 |
| 普通话↔英语双向翻译 | 两个 Realtime 会话及音频路由已实现 | 真实模型权限、音频、听感、打断和回声测试 |
| 字幕、本机可选历史和导出 | 实际事件驱动，默认不持久保存 | SSE 断线不补发；异常关闭可能缺记录 |
| 私密配置和访问保护 | 字段校验、私有文件 ACL、本机访问凭据、回调签名校验已实现 | 真实凭据和 Twilio 成功签名/媒体流验收 |
| 本机运行 | 本次开始时 5050 未监听；已用正式启动器恢复，`/health` 为 200，鉴权状态接口为 200 | 保持服务运行；当前仍未配置，不能取得通话令牌 |
| 公网隧道 | 本次重新启动 Cloudflare 临时隧道并保存新地址；公网健康探针为 200 | 临时地址变化后仍需重新准备/验证/改绑；没有自动恢复 |
| 公网访问边界 | 公网 UI/状态为 403；无签名语音 POST 和媒体 WSS 均为 403 | 拒绝未授权访问不证明真实 Twilio 能接通 |
| 编译和离线测试 | 本轮 `npm run build` 成功，`npm test` 为 54/54 | 模拟测试不代替真实 API 和电话 |
| 静态检查 | solo ESLint、scripts TypeScript、5 个 PowerShell 脚本解析、浏览器 JS 语法检查通过 | 全仓库 lint 仍有旧 Flex 文件问题，见下文 |

本轮只验证界面 HTTP 可达和已有启动入口，没有重新取得浏览器视觉操作结果；先前的隔离浏览器 UI 检查属于历史证据。

## 3. 2026-09-22 供应商缺项与当时计划（历史）

`npm run check:solo` 本轮实际输出以下 7 项 `missing`，只记录名称，不记录秘密值：

| 供应商 | 缺少设置 | 用途 |
| --- | --- | --- |
| Twilio | `TWILIO_ACCOUNT_SID` | 账户身份 |
| Twilio | `TWILIO_AUTH_TOKEN` | 账户 API 与回调签名 |
| Twilio | `TWILIO_CALLER_NUMBER` | 用户已有、已授权复用的语音号码 |
| Twilio | `TWILIO_API_KEY_SID` | 浏览器电话令牌所用 API Key |
| Twilio | `TWILIO_API_KEY_SECRET` | 与上述 Key 配对的 Secret |
| Twilio | `TWILIO_TWIML_APP_SID` | 浏览器电话使用的 TwiML App |
| OpenAI | `OPENAI_API_KEY` | Realtime API 认证 |

前三项 Twilio 基础信息及 OpenAI 密钥须安全配置；Twilio API Key/Secret 和 TwiML App 可由已有准备脚本创建或复用。不是要求用户手工寻找七把密钥。

`PUBLIC_BASE_URL`、默认模型名及本机访问保护通过格式检查；模型名格式就绪不证明账户具备模型权限。`configure:twilio` 本轮默认只读运行返回 `blocked`，没有创建供应商资源、改绑号码或发起电话。

必须执行：

1. 在本机安全完成凭据配置；不得提交 `.env` 或把秘密写进聊天。此前 OpenAI 本机保存确认未获批准，后续仍应走其规定流程。
2. 保持本机服务及公网 HTTPS/WSS 运行，运行 `configure:twilio -- --prepare` 准备 Key/App。
3. 运行 `verify:providers`，取得真实 Twilio 检查和 OpenAI `session.updated`。此检查不验证真实电话，也不代替号码路由核验。
4. 执行 `configure:twilio -- --apply`，将已获用户授权的号码改绑到新回调并读取远端核对。原语音配置有私密备份，短信路由保持不变。
5. 使用用户指定的测试号码验收外呼和来电：两个翻译方向、字幕、静音/拒接/忙线/未接/掉线、任一侧挂断及计费停止。
6. 测量短长句、数字姓名、连续讲话与打断，从说话结束到对端听见翻译的真实延迟，记录实际用量和费用。

## 4. 除配置以外的实现缺项与限制

- **隧道恢复仍需手工操作。** 桌面启动器只启动本机服务，不自动启动公网隧道，也不自动更新变动的 Twilio 回调。当前快捷方式不能等同于重启电脑后全链路一键恢复。
- **字幕缺少断线补发。** `server.ts` 的 SSE 初始快照只有通话状态，没有事件序号或字幕历史重放；浏览器失去事件连接期间的字幕不能保证恢复。
- **通话中按键菜单未实现。** 通话时拨号键禁用，没有向对端发送 DTMF 的完整流程；无法使用“按 1 转人工”等客服菜单。它属于日常业务电话的重要扩展，不能说已经支持。
- **指标尚未完整接入。** 翻译桥有可选首音频局部耗时回调，但会话管理器未接入该回调；未记录模型 usage 或生成实际费用报表。首音频局部指标也不能替代双端听见延迟。
- **缺少自动回滚命令。** Twilio 原语音设置保存在本机私密备份，目前需要人工核对恢复。
- **全仓库 lint 未通过。** 本轮 `npm run lint` 报 1062 个错误、0 警告，均在旧 Flex/src 非 solo 文件；1059 项格式问题，另有 `AudioInterceptor.ts` 的 3 项规则问题。solo 定向检查通过，不将全量 lint 描述为通过。
- WhatsApp、其他翻译供应商及原生桌面安装包未实现。云端部署当前不在用户选择的本地运行范围内，不能强制当作当前接通的前提。

本轮修正了一个已确认的设置问题：API 验证前端等待时间原为 45 秒，而后端四个顺序步骤最多约 60 秒；调整为 75 秒，避免在后端仍正常等待时过早提示本机超时。这不代表已验证真实慢网或供应商连接。

## 5. 正式完成的判定

仅在取得真实 API 验证、已授权号码改绑结果、真实外呼与来电、双向翻译及挂断测试记录后，才能称为基础电话链路接通。延迟、稳定性、字幕恢复、按键菜单和费用能力分别记录，不能由某一次电话成功推断全部完成。

本次推送仅包含项目代码和非秘密文档；`.env`、`.runtime/`、日志、临时公网地址、供应商凭据和浏览器生成的历史不随仓库同步。
