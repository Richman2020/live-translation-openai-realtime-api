# 项目进度与交接

更新日期：2026-09-26。共享仓库：<https://github.com/Richman2020/live-translation-openai-realtime-api>。

需求见 [PROJECT_BRIEF.md](PROJECT_BRIEF.md)，工作规则见 [AGENTS.md](AGENTS.md)，运行步骤见 [LOCAL_SETUP.md](LOCAL_SETUP.md)，后续执行入口见 [LOCAL_CODEX_HANDOFF.md](LOCAL_CODEX_HANDOFF.md)。

## 最新优先级与外部方案研究（2026-09-26）

- 用户要求先解决约 3 秒耳听等待，并覆盖长句和连续发言；此前“2 秒以内”不是最终要求，不靠缩短测试话术交付。准确性、完整性仍是候选门槛。
- 已对照远端 `e40a8829` 的实际 solo 代码：最终 ASR 是翻译启动门槛，生成音频已经逐块即时转发。已有日志排队中位为零，不能直接归因于队列或网络。
- 已核查专用 OpenAI 连续翻译、Palabra 官方 Twilio 示例与流式 API，以及 SimulStreaming、SeamlessStreaming、Hibiki 系列。建议保留当前引擎，先验证 OpenAI 专用接口的账户与中英双向能力，再与 Palabra 对照；速度和质量未实测，尚未选定替换供应商。
- 可执行接入顺序、媒体转换、完整性要求及长句耳听比较方法见 [连续翻译方案研究](STREAMING_TRANSLATION_OPTIONS_2026-09-26.md)。本机运行决定保留，未部署云端。
- 本轮只改研究与交接文档；未读取或创建密钥、调用新模型、重启或拨号。文档内容检查与远端回读用于验证此次同步；此前 195 项测试不代表新引擎已经通过。
- 下一步本机 Codex 从本分支读取上述方案，先做默认关闭的独立引擎适配与重复输入对照，通过后再进行双方准备好的真实电话比较。代码仍未合入 `main`。

## 最新质量分析（2026-09-26 09:58–10:00 UTC 通话）

- 用户报告识别仍不准、声音短、耳听仍约 3 秒；本轮仅分析，未改运行配置、重启或另行拨号。详见 [识别与延迟分析](LATENCY_ACCURACY_REVIEW_2026-09-26.md)。
- 真实日志 20 段：电脑 → 手机与手机 → 电脑的停说事件到首音频中位分别 1273/1394 ms，转写等待 406/456 ms，生成首音频 888/798 ms；排队中位均为零。它们不覆盖 VAD、完整传输和实际耳听，不能直接推算网络占比。
- 电脑 → 手机 8 段中 7 段有播放完成确认，1 段结束时未确认；手机 → 电脑 12 段均确认。不证明音量、完整性或语义正确，未保存源音频，不能给出准确率或唯一误识别根因。
- 官方已核实专用 `gpt-realtime-translate`，推荐独立评估；不是修改模型名即可替换。账户可用性、中英质量、实际耳听速度均未验证，未调用该新 API。
- 下一步固定设备和 V1 稿、补真实输入与播放证据，再独立适配及比较专用翻译。本次只改文档，执行差异检查；195 项测试为此前源码版本结果，不能代替新候选验收。

## 此前恢复（2026-09-26 09:33 UTC）：无法拨号的两处实际阻塞

- **本机服务退出**：用户报告不能拨号时，页面显示本机服务未连接，5050 无监听，旧 worker/server 均已不存在。stderr 为空，未找到相应崩溃或电源事件，不能追溯准确退出原因。当前工具进程实测位于带 `KILL_ON_JOB_CLOSE` 的 Windows Job，旧直接 `Start-Process` 存在继承并被回收的风险；这项结构风险已证实，不等于已证明旧进程最终由谁终止。
- **启动器修复**：本机 worker 和 cloudflared 改由本地 Windows CIM 隐藏启动，保持现有用户身份、无服务安装/计划任务。正式本机实例实测 worker/server 均 `IsProcessInJob=false`，同次工具进程对照为 true；保留 PID、开始时间、后代身份及清理确认。新 worker 的安全 Stop、5050 释放、Start 和再次 Start 复用同一实例均通过；worker 启动错误单独留本机日志。
- **独立的公网失效**：旧 cloudflared 进程仍存活，但最后成功注册为 07:27:17Z；07:54:08Z 起服务端持续 `Unauthorized: Tunnel not found`。09:24:28Z 本地健康 HTTP 200、公网 HTTP 530 / Cloudflare 1016。此次不能沿用更早 1033 或仅归因 TCP 超时，也不能把进程存活当作隧道可用。
- **隧道和回调已恢复**：确认无通话后正式停止旧隧道、新建独立进程；旧日志保留，新日志由 cloudflared 自身写入，避免调用者管道关闭或误读旧地址。配置计划确认无需新 key/app/号码，`configure:twilio --apply` 经供应商检查后返回 configured，并回读原号码和原 TwiML App 新回调。09:33:41Z 本机配置就绪、无活动会话、仍为 gpt-4o-transcribe，公网 readiness 已 ready。未购买资源或拨出电话。
- **页面恢复**：保持原 10 秒轮询，失联后继续重试；首次加载离线也能在状态恢复后建立唯一事件连接。只清除已恢复的状态读取错误，拨号/挂断/认证错误不误清；401 或页面关闭后不重新连接。195/195 全量测试、JS 语法、PowerShell 5.1 语法及差异检查通过；浏览器加载修复页面后失联横幅消失。
- **边界**：当前已恢复拨号所需服务和回调，不代表新一通电话或识别/延迟验收通过。临时公网地址仍可能失效；重建后必须重新核验和绑定，尚未实现无人值守的自动修复。代码仍走 `codex/local-phone-workbench` 和草稿 PR #2，不等于合入 main。
- **最终复核补充**：09:21 启动实例的 worker/server 均不在 Job；后续正式重启实例中，worker 与新隧道不在任何 Job，server 在某个 Job，一次成员列表查询确认它不属于当次工具 Job，来源未进一步判定。新隧道 09:29:50Z 注册成功，当前日志无 Unauthorized/error。浏览器另发生一次 31005 信令断开后自动重新注册；已重新登记线路，不能以当前按钮恢复宣称网络长期稳定。

## 同日此前（06:43 UTC）：固定稿后的识别与等待时间

- **真实反馈与线路**：用户完成 V1 一轮后报告中文识别有偏差，英文需慢说、大声说，耳听等待约 2–3 秒。06:26–06:28Z 的 PSTN 115 秒、浏览器 124 秒均 completed；英语 6 段 / 74400 字节、中文 9 段 / 77200 字节生成、送出及真实 mark 一致，两条 OpenAI 连接未重连。正常线路结束不等于识别或听感验收。
- **识别与网络证据**：相对固定稿，中文六句中三句有实质语义偏差，后续译文沿用错误原文。未保存真人源音频，不能当作严格 ASR 错误率；私密逐句记录仅在 `.runtime/test-runs/`。该通话读取到的 23 条 RTC 样本中，接收丢包约 8–35.29%；这是下行证据，不能据此断言麦克风上行识别差的根因，线路波动尚未解决。
- **采音改动**：新增独立麦克风选择，拨出、接听及 SDK 重取音轨均保留本次选定设备，断开时不静默回退。显示实际音轨名称，未知时明示、结束清空；不自动采音、不保存设备 ID。此前只选择耳机输出无法证明输入来自耳麦，也不能武断认定历史默认设备就是根因。
- **模型比较与候选**：一次有限真实 OpenAI A/B 使用同一 12 句合成稿，两模型均保留关键语义。停说事件接收至最终转写中位数 Whisper 1068 ms、GPT-4o Transcribe 486 ms，每模型 14 个分段，不含生成及电话播放。方法、逐句对照及历史“今天→天天”失败见 [ASR_AB_2026-09-26.md](ASR_AB_2026-09-26.md)。本机私密配置已显式改为 `gpt-4o-transcribe` 作为下一轮候选；仓库默认仍是 `whisper-1`，VAD 500 ms、阈值及 prompt 不变。合成结果不代表真人质量或 2–3 秒已解决。
- **计时缺口及修复**：旧桥接器的可选 metric 未接入会话和日志，本轮真人没有可拆分 2–3 秒的历史时序。现接入会话、脱敏日志及界面，区分停说事件接收后等待最终转写、等待发起响应、生成首音频；只计首块，缺事件或时钟倒序不制造 0 ms，旧会话隔离。指标不含 VAD 静默、线路与设备播放，不能等同于耳听延迟。
- **检查与部署**：全量 187/187 测试、构建、改动后端 ESLint、前端语法及差异检查通过。正式 Stop 确认清理及安全停止后 Start 成功；06:43:05Z 无活动会话、候选模型就绪，真实供应商 4/4 和公网 readiness 通过。Computer Use 已确认新控件、电话注册及拨号可用，并在当前页面选择 USB 耳麦实体输入/输出；尚未启用音轨或拨号，实际采音与人耳效果待真人测试。
- **下一步**：保持同一 V1 稿、耳麦与手机听筒、关闭外放，正常音量逐句记录识别和耳听等待，结合新增时序定位。设备固定与候选模型同时进入下一次现场测试，整体改善不能单独归因模型；必要时同设备做单变量对照。当前分支 `codex/local-phone-workbench`、[草稿 PR #2](https://github.com/Richman2020/live-translation-openai-realtime-api/pull/2)，尚未合入 main。

## 同日此前：新增固定双人测试稿 V1.0

- 按用户要求建立 [PHONE_TEST_V1.md](PHONE_TEST_V1.md)，使用 12 句初级中英话术（每个方向 6 句），覆盖双向有声、问句不代答、姓名、今天/明天、下午三点、数量及否定；另有三个静默片段观察迟到、回录或额外内容。
- 每轮固定顺序和句子，接收方分别报告耳听与字幕；附逐句记录模板、无声时的两向隔离检查、读稿偏差及未测标记、单因素改动和版本规则。私密结果存 `.runtime/test-runs/`，共享仓库不保存真实号码或真人录音。
- 本轮仅编写并检查文档，未拨号、未调用模型、未修改运行程序或配置。旧 163 项测试和供应商证据不代表新稿已执行；新稿真实结果待两人准备后取得。

## 当前排查（2026-09-26）：手机英文已听到，电脑中文输出与翻译越界分别处理

- **最新用户反馈与线路**：`04:01:50–04:03:51Z` 浏览器线路 120 秒，PSTN 113 秒，均 completed；两媒体流正常结束，TwiML 200、状态回调 204、PSTN SIP 200，Notifications 空。英语 6 段 / 106000 字节、中文 4 段 / 47200 字节的生成、送出和实际 Twilio mark 均一致，无未确认段；两个 OpenAI 会话没有重连或关闭。用户确认手机能听到英文且大部分准确，但电脑耳机没听到中文，并出现未说过的话。线路 mark 不能覆盖这一听感失败。
- **证据分层**：浏览器 RTC 日志有非零接收音量，且部分采样存在丢包；尚不能由此认定耳机在播放。用户明确确认测试时手机免提或其他外放，真人记录中有刚生成的英文再进入本地原文，支持声学回录；不能据此把所有异常都定为回声。共享文档不保存真人对话、设备 ID、号码或账号标识。
- **确认的翻译生成问题**：已有目标语言问句被模型回答为自述翻译角色。首轮仅加强提示词后，真实 OpenAI 4 个合成文本对照仍有 1 个目标语言请求触发“请换成中文再说”的扩写；这是不经过麦克风/ASR 的独立失败。现将 source_text 通过 JSON 序列化封装，明确其为待译数据，目标语言原样输出、混合语句保留完整含义、问句/请求不执行。该结构不是安全或质量保证，最终实测单独记录。
- **电脑输出修复**：新增 SDK 输出设备选择及回读、同源中文 WAV 试听，不调用电话或麦克风、不保存设备 ID。试听只在通话外可用，播放结束/错误/取消/超时均释放播放器；切换超时保留锁，重新开启通话可退休旧操作，晚到结果不能改新设备。基于公开 `call.on('audio')` 汇总活动播放器，不误把 SDK 暂时输出的暂停视为整个通话暂停；通话结束移除监听。仍需用户确认耳机实际听到。
- **未改动**：默认模型、VAD 与转写设置保留；没有用固定词、低音量阈值或语言过滤擅自删句，没有自动拨出新电话。后续电话需耳机、手机听筒和关闭外放后做双向固定句对照。
- **结构封装的真实对照**：`04:17:25–04:17:33Z` 精确使用当前源码 prompt/envelope 的原 4 个合成文本样本均符合预期；此前失败的英文请求本次原样输出，两个目标语句保持原样，混合句保留姓名和明天下午三点。4 段音频非零，两连接均正常 1000 关闭；没有 Twilio 电话。旧失败和新请求证据均保留在私密 `.runtime/quality-check/`。这只证明这组样本，不证明普遍稳定、ASR 或电话听感通过。
- **代码和部署验证**：全量 **163/163 测试通过**，构建、两处改动源码 ESLint、前端 JS 语法通过。正式 Stop 返回线路清理及安全停止确认后 Start 成功。`04:17:36Z` 运行配置就绪、whisper-1、无活动会话，真实 API **4/4** 和公网 readiness 通过。Computer Use 刷新确认新控件和注册，实际同源中文试听从播放到 ended 完成，浏览器无 warn/error；这不等于人耳已听到。
- **当前输出设备线索**：页面实际只枚举显示器音频（默认/通信/实体项均指同一显示器），尚未发现独立耳机项；已询问耳机接法与试听听感。耳机若插显示器可能本就走该端点，若插电脑则需让系统识别正确输出，不能仅凭设备名定案或擅自改麦克风。
- **系统只读交叉核验**：Windows AudioEndpoint 清单显示旧 USB 耳机的播放/麦克风端点均 `Present: false`，显示器播放端点和摄像头麦克风为 `OK / Present: true`。这与浏览器枚举一致；没有擅自启用/禁用设备、改系统默认值或更换当前有效麦克风。需用户重新连接实际使用的耳机或确认其接在显示器上，再确认本地试听。

## 同日此前证据（截至 03:53 UTC）

- **获批地址更正路径核对**：本轮精确 API 仍为同账户 `twilio-approved`，关联地址国家/法定地址不一致仍存在。官方 [Address API](https://www.twilio.com/docs/usage/api/address#update-an-address-resource) 不允许原位修改 `IsoCountry`；[Primary Profile 状态表](https://www.twilio.com/docs/trust-hub/profiles/primary-compliance-profiles) 将 Approved 标为不可编辑，[官方 FAQ](https://help.twilio.com/articles/26134326277787)要求联系 Support。已在私密目录准备针对获批现状的咨询草稿，仅含必要记录标识，不含税号、密钥或附件；等待明确发送授权，尚未发送。需让 Support 确认具体字段需要法定注册地址还是实际经营地址，不能把美国注册代理地址当作实际办公室，也不能直接套用国际号码 Bundle 复制流程。未更改或重置现有获批资源。
- **03:53 UTC 只读复核**：本机就绪、无活动会话或未确认清理，公网 readiness 正常，最新电话仍是下述 03:35–03:36 的一通；没有新增电话或付费模型测试。Twilio 网页的 AX/新页读取超时后，通过截图确认其提示因闲置退出登录；本机网页读取正常且电话仍注册。后续查看/发送需要恢复该登录，并不表示电话 API 或企业审核失效。
- **03:35–03:36 UTC 最新真实电话**：用户自行测试，PSTN 61 秒、浏览器线路 68 秒；实际 Twilio mark 确认英语 14 段/289200 字节、中文 5 段/39200 字节。local 方向共生成 15 段/318400 字节，另记录未确认 1 段/29200 字节及一个 0 字节项目；不能把未确认部分也计为已播放。请求/回调为 200/204，无媒体流错误。这比“仅有模拟播放确认”多了一层真实线路证据，但仍不等于用户实际听清或译文正确；不复制真人字幕到共享文档。
- **当前未定原因**：真实会话有疑似译音回录和 ASR 异常，尚未确定是声学串音、真实背景声音还是识别幻觉；未发现足以认定代码直接回灌音频的证据。不得以重复字幕单独定因，也没有新增自动删句、按语言强拦截或半双工静音。
- **采音修复与真机检查**：预检及 SDK 实际获取统一请求回声消除、降噪、自动音量的 `ideal: true`，只回读三项布尔实际设置，缺失显示未知；保留显式设备选择，取消和新通话清空旧状态。真机本地检测 20 秒，三项均 true，RMS 峰值 0.09011、1201 帧中 76 帧有声，音轨已释放；没有录音或电话。设置启用不证明能消除旁边手机外放。
- **转写模型决定**：新增显式 `OPENAI_TRANSCRIPTION_MODEL`，最终默认保留 `whisper-1`，可选 `gpt-4o-transcribe` / `gpt-4o-mini-transcribe`；供应商必须准确确认所选模型，不自动回退。`03:46:17Z` 的 gpt-4o 候选真实 OpenAI 已接受，4 句都有非零音频，但第一句把“今天”转写为“天天”，继而译为 every day；其余 3 句文本正确。因此不能因模型更新就默认替换，也不能把先前 Whisper 文本路径的 4 句 fixture 通过推广为普遍可靠。对照均无 Twilio 电话，原始结果仅在忽略的 `.runtime/quality-check/`。
- **本轮检查与加载状态**：完整 `npm test` **138/138 通过**；默认恢复 `whisper-1` 后 4 项受影响测试、最终构建和 app 语法再次通过，定向 4 个源码文件 ESLint 通过。`03:48Z` 正式 Stop 取得清理与安全停止确认后 Start 成功；`03:49:26Z` 私密回读确认运行模型 whisper-1、新设置检查 ready、`activeSession: null`，真实 `/api/verify` 4/4 通过（OpenAI 严格确认模型后 `SESSION_UPDATED`），公网 readiness 为 ready。Computer Use 已确认电话已开启、已注册可接收来电、拨号按钮可用、新诊断说明可见；工作台和本机诊断页已保留。提交推送以 Git 核验为准。

- **用户实际反馈**：开始时中文对应译文正确，但手机没有听到翻译语音，之后翻译出现错乱。此前双向字幕、非零浏览器音量和正常结束只证明局部链路有数据，不能据此称语音或质量验收通过。当前需分别核对翻译音频生成、向目标电话发送及实际播放，以及固定句的转写/译文准确度。
- **本轮已实现的公网拨号门禁**：创建本地外呼会话前，先用有期限、限制响应大小的只读探针核对公网 `/api/health` 应用标识；入口不可达或服务不符时返回 503，不创建会话、供应商电话或媒体连接。检查期间拒绝并发拨号、配置修改及供应商验证，结束或异常后释放门禁，允许重试。此举防止已知入口失效时继续拨号，不能保证探针之后的隧道稳定性。
- **本轮已实现的音频分段诊断**：按方向区分生成字节、写入目标连接字节、Twilio `mark` 播放确认及未确认状态，界面分别计数，服务日志只保留方向、阶段和字节数。写入成功不等于播放；真实线路的 mark 也只表明该段已排出播放队列，不证明用户实际听见。已有上述真实线路记录，仍需与手机实际听感对照。
- **首次非电话合成语音结果**：`03:26:09Z` 的真实 OpenAI 对照完成 4 句，均生成非静音音频，生成、发送与模拟接收字节一致，连接全部清理；未拨 Twilio，播放确认明确为 `SIMULATION_ONLY`。旧版直接引用原始音频 item 的第一句，ASR 正确为“你好，请问你今天几点下班？”，英语却将“今天”译为 “every day”，确认存在语义失真；其余 3 句的文本对照正确。它证明测试条件下音频生成与桥接写入有效，不能解释或排除真实手机无声。私密原始结果位于忽略的 `.runtime/quality-check/native-audio-result.json`。
- **已完成最终 ASR 文本路径**：现在等待该轮最终 ASR，再以相同原文文本独立生成单句译文和语音；旧音频 item 直译仅保留为上述历史对照。这样统一字幕与翻译输入，代价是增加最终转写等待，且 ASR 错误仍会传入译文，不能保证任意真实对话都准确。
- **第二轮非电话对照**：`03:31:23Z` 完成 4 句、4 次 response，全部保留本组语义，包括 today、问句、不取消、明天下午三点和五点。每句生成/发送/模拟接收字节分别一致为 22400、30800、10000、19200，均非静音且目标方向正确；全部连接清理。整轮约 29.67 秒，**不是单句或端到端延迟**。原始结果在忽略的 `.runtime/quality-check/grounded-text-result.json`；模拟播放确认仍为 `SIMULATION_ONLY`，没有 Twilio 电话或真人耳听。
- **03:35 前版本的验证与运行历史**：当时完整 `npm test` 125/125、构建、前端语法、定向 5 文件 ESLint 和差异检查通过；正式 Stop 确认清理后 Start 加载，浏览器重新注册、拨号可用、诊断面板可见，公网 readiness 为 ready。随后才发生上述 03:35–03:36 真实测试；该历史记录不覆盖本轮新增采音和模型选择修改。
- **03:05 UTC 的结束原因**：该次 PSTN 通话持续 7 秒，由用户本人手动挂断。同期 connect/status 回调出现 HTTP 530 是另一个公网入口故障，不能用回调错误覆盖用户主动结束这一事实。
- **03:15–03:16 UTC 的三次失败**：用户于 `03:15:25Z`、`03:15:39Z`、`03:16:01Z` 发起的拨号仅有浏览器线路，均为 `busy`、0 秒，未创建 PSTN 线路；页面出现 `31603`。实际 `/voice/client` 请求全部 HTTP 530，响应正文确认 Cloudflare `1033`，Twilio 为 `11200`；账户仍 active。这三次是 TwiML 入口无法到达，不是已证实的重新 KYC 拦截或目标手机占线。
- **隧道证据与恢复边界**：隧道日志在 `03:06–03:12Z` 记录 TCP 7844 连接超时；`03:17:10Z` 自动重新 registered，期间没有人工重启操作，`03:18:07Z` 公网健康检查返回 200。Windows 日志未证实休眠，不能将休眠写成根因。健康恢复只证明该时刻入口可达，未证明隧道持续稳定或用户无语音问题已解决。

- **企业审核**：当前官方 Trust Hub 页面显示 Primary Business / **Approved**；按当前页面档案标识精确读取 API，同一账户返回 `twilio-approved`。当前档案标识已变化，旧档案精确读取为 404，不能继续使用旧档案或列表遗漏判断当前审核状态。关联法定地址的国家仍为 `CN`、`validated: false`，与已核对的法定地址不一致；审核获批不能证明全部资料正确。地址更正问题仍待处理，本轮尚未向支持发送消息。
- **本机运行恢复**：本轮开始时本机服务已停止、旧临时隧道失效。现已按正式启动流程恢复服务并建立新临时隧道；真实供应商验证 **4/4 通过**，原有号码和原有 TwiML App 的语音回调已改绑并经远端回读核验。本机鉴权接口、公网健康检查及未授权访问边界也已复核。临时隧道仍无重启自动恢复和自动改绑能力。
- **最小电话对照**：向用户已授权目标执行受控直接 PSTN 测试，Twilio 接受请求，状态依次为 `queued` → `ringing` → 20 秒无人接听后的 `no-answer`；没有再次返回 `21216`。这证明该次目标外呼已不再被原拒绝拦截，不证明接听、音频或翻译通过。
- **第一通真实工作台通话**：用户确认可以接听后，工作台通话于 `2026-09-26T01:46:19Z` 接通，约至 `01:47:17Z`、SSE 接通窗口约 58 秒。Twilio 最终记录浏览器线路 65 秒、PSTN 线路 59 秒，两者均为 `completed`；两个媒体流都出现 `started` → `stopped`，没有流错误，状态回调为 204、TwiML 为 200。实际 SSE 收到对方（`remote`）原文 4 条和译文 4 条，未收到我方（`local`）字幕，但不能据此推断我方媒体流没有建立。
- **已定位的结束触发**：恢复原始 SSE 失败事件后确认错误为 `openai_connection_closed:remote`，直接触发应用结束的是对方对应的 OpenAI WebSocket 关闭；具体关闭原因仍在排查。UI 同时出现的 `31005` 属于浏览器信令错误，不能替代上述原始失败原因。旧会话已结束，幂等挂断回读确认无待清理线路、`activeSession: null`。未把单向字幕结果称为双向音频或翻译验收成功。
- **第二通真实结果**：修复版本的第二通于 `2026-09-26T02:36:02Z` 结束；Twilio 浏览器线路 146 秒、PSTN 线路 127 秒，两者均为 `completed`。双媒体流 `started` → `stopped`，无流错误或 Notifications，回调 204；后端正常 `completed`、`error: null`、无待清理线路。UI 收到对方原文 11 条、译文 11 条，我方仍为 0；期间没有 OpenAI 关闭。SDK 报告 `low-bytes-sent`、高丢包、恒定输入和低 MOS 警告，不能把正常结束和对方字幕称为双向音频验收。
- **我方采音核验**：挂断后单独检测原默认耳机麦克风 20 秒，RMS 为 0、没有有声帧。随后 Windows/浏览器当前可用设备已换为 USB 摄像头麦克风，指定该设备检测 20 秒，RMS 峰值 0.11337、1202 帧中 756 帧有声，证明当前本机采音成功；随后刷新工作台重新获取当前默认麦克风并完成第三通。
- **第三通真实结果**：Twilio 浏览器线路于 `2026-09-26T02:43:22Z` 开始、持续 150 秒；PSTN 线路于 `02:43:42Z` 开始、持续 130 秒，两者均为 `completed`，按开始时间和时长计算结束于 `02:45:52Z`。`02:47:56Z` 只读检查为 `activeSession: null`。完整 UI 有我方原文 25 条、译文 25 条，对方原文 12 条、译文 12 条；日志两个 OpenAI 会话均 ready，期间未再次关闭。浏览器发送约 8000 字节/秒、接收 6880–8000 字节/秒，输入/输出音量均曾非零，采样丢包 0–14%。这些证据证明双向输入和字幕链路已有实际数据，不证明用户耳听、翻译准确度或网络稳定性通过。
- **第三通后已实现的上一版修复**：曾将 session 和每次 response 的翻译提示统一到共用函数，加入保留问句、指代、否定和数字的例子；UI 按 `role + itemId + contentIndex` 逐句配对原文/译文，实时单行插入或替换，历史记录与导出保持一致，并修复陈旧的待清理提示。该组修改当时通过代码验证且已安全加载，页面回读为「电话已开启」、拨号按钮可用；其后上述合成测试仍发现“今天”误译，不能认为只改提示词已解决语义偏移。音响回录、自激及真实电话转写差异仍是独立待验证候选。
- **字幕显示局限**：当前原文/译文已按共同 item 正确配对；整组顺序仍取该组首个字幕事件的到达顺序，异步转写较晚可能使整组显示次序不同于实际说话顺序。此处没有实现跨线路共同声学时间排序，也不将显示顺序问题直接归为模型误译；本轮先保留这一边界，不扩大排序实现。
- **尚未验收**：用户已明确报告手机未听到翻译语音，语音验收未通过；翻译质量、端到端延迟、网络与临时隧道稳定性、真实来电及费用仍未完整验收。需要耳机隔离声音，并使用固定句中英两向对照；企业审核、API、本机采音、接通和双向字幕不能替代该项验证。
- **下一步**：等待用户对最新通话的实际听感反馈；准备好耳机、手机听筒及关闭其他外放后再做中英交替固定句对照，不新增无准备电话。核对原文、译文、分方向字节与真实 mark，记录准确度和端到端等待；不要把 AEC true 或换模型当作已修好。第一通 OpenAI 关闭原因未复现，获批档案地址国家差异亦待处理。本轮提交推送以完成时 Git 核验为准；开发分支 `codex/local-phone-workbench`、草稿 [PR #2](https://github.com/Richman2020/live-translation-openai-realtime-api/pull/2) 不等于已合入 `main`。

### 同日受控恢复修复与验证

- **后端恢复**：已就绪的 OpenAI 会话遇到允许的传输或服务关闭时，每个方向最多重建一次；政策、认证、格式和配置错误不重试，第二次断开则结束并清理电话。重建仍有 10 秒握手期限，只保留最近 2 秒待发输入，不重放旧 turn、旧 item ID 或已生成语音。旧连接事件及延迟写入回调不会影响替代连接；独立审查发现的 `CLOSING` 先于 `close` 竞态也已修复，等待真实关闭码期间按上限缓冲，并保留关闭等待期限。
- **界面恢复与诊断**：分别显示翻译连接与 Twilio 浏览器信令恢复状态；翻译中断后提示重说，两个方向状态独立，忽略旧会话事件。浏览器信令给 SDK 最多 30 秒恢复窗口；脱敏日志只记录必要状态和错误码。第二通后又增加基于 SDK `volume` 的声音检测、独立网络质量提示，以及每 5 秒一次的纯数值 RTC 诊断，帮助区分麦克风无输入与网络问题。
- **上一版本代码验证**：受控恢复、声音/网络诊断、统一翻译提示、字幕配对及清理提示修复当时的 `npm test` **92/92 全量通过**；`npm run build`、前端语法、定向 ESLint 和 `git diff --check` 全部通过。此前 86 项全量及 23 项定向是已被该次验证覆盖的中间记录。独立复核覆盖关闭窗口内到达的音频、两个方向同时恢复、传输写入错误先于关闭，以及退休连接的延迟回调。该记录不覆盖用户新反馈后的新增变更，也不证明电话音频或翻译质量通过。
- **真实 OpenAI 对照**：有客户端 ping、无客户端 ping 两组真实静音连接均正常保持约 88 秒，已有服务端 ping；没有复现第一通断开，不能把原因认定为固定空闲超时或缺少客户端 ping。另用真实 bridge 和两个真实 OpenAI 会话做故障注入：两端 ready 后强制断开对方连接（1006），约 **1.716 秒**后替代会话 ready，内存电话连接持续 OPEN，最终全部 3 个 OpenAI WebSocket 均确认清理。该测试没有真人音频、`response.create` 或 Twilio 电话，不能称真实双向电话恢复验收。
- **本机修复版本**：受控恢复版本已正式执行 `Stop-AIPhone.ps1`，取得线路清理与安全停止确认后再执行 `Start-AIPhone.ps1`。第二、三通真实工作台测试与上述故障注入分别记录，不以服务重启成功推断电话效果；第三通后的提示词及字幕配对修改已通过最终代码验证，最终版本已安全加载，页面回读为「电话已开启」、拨号按钮可用。

## 2026-09-24 历史记录：企业主档案已提交待审，地址国家不一致已确认

### 企业认证提交结果（同日后续）

- 已通过真实登录后的 Trust Hub 页面发现一个被拒绝的 Business Primary Profile，错误为 `18602`：`The Business ID you provided could not be verified`。旧表单填写个人名称、Sole Proprietorship、Other 注册类型及旧地址/网站。旧版列表接口仍只返回获批 Individual Primary；按网页企业档案标识精确读取则返回同账户的企业档案，证明该列表不能完整反映新版企业主档案，不能仅将差异解释为查询时间不同或认定当前没有企业档案。
- 用户提供的公司注册、企业税号、地址服务和注册收据文件已用于核对；进入现有档案的 Edit and resubmit，按文件更正公司法定名称、EIN 注册类型及编号、LLC 类型和网站，核对代表及联系信息，代表职务填为 Sole Member / Other。没有另建主档案或用代办签署人代替公司所有人。
- 实际表单两处要求填写与企业注册编号关联的法定地址，因此使用已核对一致的企业税号信与注册文件法定/邮寄地址。该地址未被声明为实际美国办公地址；用户已确认的真实经营地址继续单独保存在本机私密材料中，本次流程没有出现实际经营/办公地址问题。
- Persona 起始页明确要求同意生物识别信息处理及最长三年的保留期；已就该具体提示取得用户确认，再点击 Next 继续。本次流程没有要求自拍或上传证件，不将点击同意等同于已完成生物识别验证。
- 已完成提交和 Finish，主档案页面显示 Business / **Pending review**，详情页显示 **In review**；精确 API 读取同账户档案也返回 `pending-review`。法定名称、企业编号、LLC 类型、网站和代表字段回读正确，但表单选择的法定地址国家没有保留：详情页与关联地址 API 均显示国家不一致，API 还返回 `validated: false`，城市则与所填法定地址一致。通知邮箱保留同账户现有联系人邮箱。仅能确认提交成功，不能称全部字段正确、审核通过或外呼 `21216` 已解除。
- 在审页面没有编辑入口，官方主档案说明也不允许编辑审核中的档案。已找到此前同类认证支持工单：先前支持回复要求提供档案 SID，工单现已关闭。补充说明已准备在原工单评论框，包含准确档案编号、更正情况、法定地址与真实经营地址的区别，以及请求核查 21216；尚未发送，等待针对 Post comment 的明确指令，不直接改写审核资源绕过流程。
- 字段、来源及身份材料仅存于受限 ACL、Git 忽略的 `.runtime/support-materials/business-profile/`，共享仓库不保存公司/个人身份字段。未读取不必要的个人税号或密码，原文件未修改。
- 重启后已实际完成网页填写和提交，但操作约需 21 秒、部分读取约 42 秒，连接仍不稳定。进程诊断确认当前 Codex/CUA 未继承代理环境；独立代理探针通畅，不能因此宣称当前进程已通过代理稳定控制网页。未更改系统代理、注册表或插件代码，也未强制结束其他任务。
- 本轮仅变更供应商档案及交接文档，未修改应用代码、创建密钥/号码、拨号或发送支持消息。下一步处理已确认的地址国家更正问题并跟进审核；审核通过或供应商确认放行后才对已授权目标受控复测。代码测试不作为此网页提交的验证证据。文档同步以完成时 Git 核验为准，PR #2 保持草稿。

### 先前拒绝原因排查（提交企业主档案前的历史快照）

- 新一轮已核对原账户、主/子关系、历史通话、地理拨号权限、主档案及其关联地址，并执行一次不连接翻译服务的最小外呼对照。原始响应仍为 `Account not allowed to call [approved test destination]`、21216/HTTP 400，未创建目标电话。
- 用户此前接通的说法有多次历史呼入完成记录支持；查到的 11 条历史外呼均失败，尚无此前成功外呼的证据。当时接口返回的获批主档案地区与用户描述存在差异，与官方特别认证要求有关联，但具体开户分类及命中规则仍需确认；没有把推断认定为最终原因。
- 完整诊断、官方解除路径和证据边界见 [TWILIO_21216_DIAGNOSIS.md](TWILIO_21216_DIAGNOSIS.md)。私密支持草稿已在本机受限目录准备，未发送；此排查阶段没有更换号码、重建密钥、扩大高风险拨号权限或填写身份资料。当时等待真实资料确认，后续已按本页最新记录完成企业档案更正与提交。
- 本轮仅更新诊断文档，未修改应用代码；起始分支已安全同步其他会话的 `b8ef59a`。本轮提交和远端一致性以完成时 Git 核验为准，PR #2 保持草稿。
- 浏览器控制连接另查得当前启用插件缺少此前的代理环境配置；已备份并只补回代理相关环境项，独立 Node 连通探测通过，但当前进程读取 Twilio 页面仍超时。标签页清单显示登录入口已打开，尚未完成新登录或控制台操作验证；需新建插件连接或重启 Codex 后复核。此故障不解释服务器端 21216。

### 同日 08:54 复测记录

- 用户已说明账户是美国账户，并要求复用此前注册的 Twilio 号码再次测试。不能据此继续把缺少 Business Primary Profile 当作已证实的失败原因；具体账户或目的号码拦截规则仍需 Twilio 核实。
- 08:55（北京时间）实时只读核对：运行配置与本机私密设置使用同一个原有号码，该号码确实由当前账户持有且语音能力开启；号码入站回调和 TwiML App 浏览器回调均匹配当前公网入口及 POST 方法，公网健康探针 200。没有购买、更换号码或修改号码配置；具体号码及账户标识不写入共享文档。
- 08:54 通过真实工作台再次拨打用户已授权目标。麦克风预检及浏览器线路成功，供应商再次返回 `providerErrorCode: 21216` / HTTP 400。Twilio 浏览器腿持续 2 秒后结束，目标手机腿未创建；最终清理确认、`activeSession: null`，未收到字幕，不能称手机通话或翻译验收通过。
- 本轮只更新复测与交接记录，未修改应用代码或重建供应商资源。起始开发分支 `305b886` 已与远端核对一致；本轮文档提交及推送以 Git 核验为准，PR #2 仍为草稿、未合入 `main`。

## 2026-09-24：Work 对 21216 阻塞的独立分析

- 本轮读取开发分支 `codex/local-phone-workbench` 的 `dd9637fc90c78f9f933bc291cd5557f1862dcf3c`、草稿 PR #2、最新交接文档及 solo 外呼源码。没有把旧 `main` 或 Work 本地的 6 项测试/占位配置当成当前电脑状态；单人工作台与供应商配置已由电脑端完成。本轮只复核代码和已有记录，没有读取本机秘密、重跑供应商检查或拨打电话。
- 根据电脑端 9 月 23–24 日的记录，麦克风和浏览器线路已成功，目标手机线路创建返回 21216 / HTTP 400，随后清理成功。源码 `src/solo/session-manager.ts` 的 `dialOther` 确实使用当前配置的账户和主叫号码调用 Twilio `calls.create`，目标参数来自本次拨号会话。当前首先要解决的是供应商接受目标外呼，翻译质量和端到端延迟尚无真实验收结果。
- `src/solo/provider-checks.ts` 的 4 项检查涵盖账户 active、号码能力、电话应用和 OpenAI 会话；没有验证一次真实 PSTN 外呼能被接受。因此“API 4/4 通过”和“外呼被拒”可以同时成立。现有 UI 仅显示安全的错误码/HTTP 状态，不能从该提示推断 Twilio 的具体拦截规则。
- Twilio 官方 21216 文档列出目的号段风险、监管和 +1 主档案要求等候选；其中 Business Primary Profile 的特别条件是 2025-10-08 起在美加之外创建的账户。用户已说明是美国账户，应核验平台记录及本次 API 所属账户，不把美国号码、US1 区域或个人档案 Approved 单独作为全部外呼条件已满足的证明，也不把缺企业档案直接定为根因。官方来源：https://www.twilio.com/docs/api/errors/21216 。
- 建议电脑端下一步：先读取既有失败请求的完整供应商说明及可取得的请求标识；在本机私密环境核对 API 的实际 Account SID、主/子账户关系和获批主档案所属账户，以及对应目的地权限。确认资料条件后若仍无法确定原因，准备 Twilio 支持材料，请其明确拒绝发生于账户、档案还是目的号码规则。材料可包含 UTC 时间、错误说明、请求标识及已存在的浏览器 Call SID，明确“目标线路未创建”；号码和账户资料仅放私密材料，不写入共享仓库。没有目标 Call SID 时不要伪造或把浏览器 Call SID 当成手机线路 ID。
- 若既有记录不足，可在用户已授权的测试范围内设计一次不接 AI 的最小 Twilio 外呼对照，记录接受/拒绝结果并结束线路；这仍是待执行建议，本轮没有实施。额外测试接听号码须由用户指定，不盲目连续重拨、打开全部高风险目的地或更换主叫号码来碰运气。
- 偶发 31005 是浏览器至 Twilio 信令连接意外断开的独立问题，应检查网络、代理、DNS、防火墙及 SDK edge；它不能解释服务器端明确返回的 21216。官方来源：https://www.twilio.com/docs/api/errors/31005 。先解除外呼阻塞，再复测线路稳定性、真实双向翻译和延迟。
- 本轮仅补充诊断与执行顺序；没有修改账户配置、提交支持工单、创建密钥、合并 PR 或宣称问题已修复。结果同步到同一开发分支，最终 SHA 以远端提交核验为准。

## 2026-09-23 排查记录

**7 项供应商设置、正式 API 验证 4/4、号码语音回调改绑及浏览器线路注册均已完成，用户麦克风授权也已实际通过。** 刷新工作台并重新注册后，两次真实拨号均到达 Twilio 浏览器线路，分别持续 2 秒和 1 秒；目标手机通话均未创建。第二次取得明确的 `providerErrorCode: 21216`、HTTP 400，当前阻塞是 Twilio 拒绝目标外呼；手机接通、来电、双向翻译音频与延迟仍待验收。

### 最新真实拨号与修复

- 独立本机诊断取得 1 条有效麦克风音轨后立即释放，不录制或上传音频。工作台旧的 `getUserMedia` 请求仍可能在权限已为 `granted` 时保持等待；确认无活动电话后刷新页面并重新注册，新的麦克风预检成功。先前 `prompt`、超时和 Twilio 无通话记录的结论仅对应下方历史阶段。
- 两次用户授权目标的真实测试均形成 Twilio 浏览器通话记录，但未形成目标手机通话。第二次在 `2026-09-23T08:45:31Z` 的服务事件中取得 21216/HTTP 400，线路清理最终确认，`activeSession: null`。未取得手机响铃、接通或翻译音频证据，不把浏览器线路成功等同于整通电话成功。
- 当时只读 API 核对：账户为 Full/active、余额为正、美国普通号码外呼地理权限开启，列表返回获批 Individual Primary Profile、未返回 Business Primary Profile，账户创建日期在 2025-10-08 及之后。9 月 24 日网页及按档案标识精确读取已证实同账户存在企业档案，而旧版列表仍未列出它；这是接口可见性差异，不能把旧列表结果等同于企业档案不存在。根据 [Twilio 官方 21216 说明](https://www.twilio.com/docs/api/errors/21216)，仍不能仅凭旧列表断言具体拦截规则。
- 后端现仅提取合法数字形式的供应商错误码与 HTTP 状态，前端针对 21216 提示核对 Trust Hub/号码限制；不将原始供应商错误及其中可能含有的私密信息附加到诊断字段。新增错误诊断与界面回归检查；最终 80/80 项测试、构建、前端语法、TypeScript、定向 lint 与差异检查均通过。
- 空闲工作台仍偶发 31005 信令连接错误；重新注册曾恢复，随后两次真实浏览器线路均已创建，但连接稳定性仍待复测。Twilio 账户主页可以读取，Trust Hub 页面操作及新开页面读取超时；审核状态已通过只读 API 获取，不能称所有浏览器或网络问题已解决。
- 服务经正式停止脚本确认线路清理后已安全重启，无需重建密钥或供应商资源。开发分支 `codex/local-phone-workbench` 的 `886b62a` 已核对远端；本次新增诊断与文档的同步结果以随后提交和远端核验为准，[PR #2](https://github.com/Richman2020/live-translation-openai-realtime-api/pull/2) 仍为草稿，未合入 `main`。

### 同日早先配置与麦克风排查记录（历史）

以下保留先前配置、76 项测试及未授权时的预检证据；其中“等待授权”“Twilio Calls 为空”等不再代表当前状态。

- Computer Use 已通过 Codex 内置浏览器实际操作 Twilio、OpenAI 及本机工作台，本轮继续完成麦克风预检、超时和取消验证。Chrome 控制本轮再次约 21 秒后 fetch 失败，独立浏览器连接诊断仍在进行，不影响已取得的内置浏览器证据。
- Chrome 独立故障只读诊断确认：应用插件同步会重写 CUA 环境并清除同名用户覆盖，此前缓存代理补丁因此不能持久保留；Codex 专用启动器继承代理仍为待重启验证的候选，本轮未改这些配置或中断应用，不能把此故障归因到当前内置浏览器麦克风准备问题。
- Twilio 账户凭据和现有号码已通过本机设置保存；`configure:twilio -- --prepare` 已真实创建并保存项目 API Key/Secret 和 TwiML App。6 项设置保存在忽略提交的本机 `.env`，不记录任何秘密值或账户资料。
- 本机服务和新 Cloudflare 临时隧道已运行，公开 `/api/health` 探针通过，当前地址已保存到本机设置。临时域名不写入共享文档；服务和隧道是否持续在线仍需下次接手时重新核对。
- 正式 `verify:providers` 在 `2026-09-23T06:05:55.183Z` 返回 4/4 通过：`twilioAccount`、`twilioNumber`、`twilioApplication` 为 `VERIFIED_RESOURCE`，`openaiRealtime` 为 `SESSION_UPDATED`。检查没有发送音频或发起电话。
- `configure:twilio -- --apply` 已返回 `status: configured`，现有号码 Voice 回调已正式接入本机服务并回读核验，SMS 路由未改动；原语音配置备份保存在私密 `.runtime/`。改绑工具本身未购买新资源、不拨号，其 `realCallTested: false` 不包含随后单独进行的 UI 拨号尝试。
- **OpenAI 密钥创建与保存已完成。** 本机 `.env` 保存目标真实获批后，官方密钥创建连接器两次返回 `OpenAI Platform rejected the API key request.`，没有错误码，原因未证实。随后按用户对网页表单的明确批准，通过 OpenAI 网页创建专用密钥并经本机配置 UI 保存到受限 `.env`；配置同步到服务内存，未为保存密钥而重启服务。秘密未输出到聊天或共享文档，无需重新创建密钥或打开 picker。
- **原审批故障已解决并留作历史。** 用户切换为 **Ask for approval** 后，本机保存确认真实返回 `approved`。此前 `approval_policy = "never"` 在显示带必填 `targetPath` 的 MCP 表单前拒绝它，0 ms 的 `decline` 不能归因为用户点击拒绝；此次未修改插件或批准逻辑。依据见 [Codex elicitation 官方源码](https://github.com/openai/codex/blob/main/codex-rs/codex-mcp/src/elicitation.rs)及[官方权限说明](https://learn.chatgpt.com/docs/sandboxing)。
- 原 OpenAI 直连曾返回 `SESSION_TIMEOUT`，裸 `ws` 连接未使用 Windows 系统代理；显式代理诊断在 2470 ms 收到 `session.updated`。共享代码新增可选 `OPENAI_PROXY_URL`，供供应商验证和实际翻译 bridge 共用，未配置时保留直连。代理已私密保存；停止脚本确认线路清理后安全重启服务，正式 4/4 验证和号码改绑均使用修复后的代码。
- 本轮最终 `npm test` 为 76/76 通过，`npm run build`、`node --check public/app.js`、`node --check public/call-lifecycle.js` 和 `git diff --check` 通过。此前代理修复的 TypeScript、定向 lint 和离线安装计划检查证据保留；它们不等于新环境完整安装或真实电话验收。
- Computer Use 已实际点击本机工作台“开启通话”，页面显示“电话已开启”“已注册 · 可接收来电”，拨打按钮可用，浏览器线路注册验证通过。随后向用户指定的测试号码发起真实 UI 拨号，75 秒后出现 `CALL_SETUP_TIMEOUT`，未取得响铃、接通或字幕证据。超时清理后本机 `activeSession: null`，无残留活动通话。
- 本轮重新核对 TwiML App 的 Voice 地址与方法匹配，公网健康探针返回 200 且应用标识正确，无签名 `/voice/client` 表单请求返回 403；最近 45 分钟的 Twilio Calls 查询为空。签名 Webhook → 媒体流开始 → 外呼创建的全内存探针通过，但供应商连接均为替身，不能计为真实 Twilio 电话。
- 独立本机麦克风诊断不经过 Twilio、不上传音频：安全上下文与 `mediaDevices` 均可用，权限状态为 `prompt`，`getUserMedia` 等待 15 秒仍未完成，证实麦克风准备受阻。Codex 应用实现支持 microphone 权限，授权提示属于标签页地址栏外层，后台标签页请求可能保持等待；不能据此声称内置浏览器不支持麦克风，也不能将 Codex 任务批准模式与网站麦克风授权混为一谈。
- 新增 `public/call-lifecycle.js`：先取得麦克风再创建后端会话，预检限时 30 秒，预备流只交给 SDK 使用一次；取消、延迟返回、旧 Device 连接及准备中的来电拒接均按所属尝试清理，避免旧结果干扰新通话。缺少本机访问令牌的错误改为全局显示，设置页也能看到重新从桌面入口打开的指引，已真机核验。
- 修复后刷新已认证工作台可再次注册。点击拨号显示“等待麦克风授权，请查看地址栏…”及“取消准备”；同时本机 `activeSession: null`，Twilio 按测试目标及浏览器身份查询均为空，未创建后端电话。约 30 秒后明确显示 `MICROPHONE_TIMEOUT` 并恢复拨号；再次准备后点击取消也立即恢复，麦克风未启用、结束按钮禁用。用户尚未回复网站麦克风授权提示，授权后的真实电话仍待继续验证。
- 本轮开始时本机与远端开发分支基线均为 `f67fb41`；开发分支为 `codex/local-phone-workbench`，[草稿 PR #2](https://github.com/Richman2020/live-translation-openai-realtime-api/pull/2) 尚未合入 `main`。本次代码与文档修改是否已推送以随后 Git 提交及远端核验为准。

## 历史下一步（2026-09-24，当前执行以顶部为准）

1. 处理已确认的 Business Primary Profile 地址国家不一致：档案在审且无编辑入口，准备官方支持更正请求，取得发送授权后提交；不要绕过审核状态直接改写资源。跟进更正及审核结果，若要求补充资料，按实际缺项处理。通过后若仍返回 21216，再核对其他账户或目标限制。Pending review 期间不重复创建档案、密钥或号码，不用连续重拨代替审核。
2. 保持并核对本机服务和隧道；临时隧道重启后地址可能变化，届时重新准备 TwiML App、验证 API 并运行 `configure:twilio -- --apply` 更新号码回调，再读取远端核对。当前尚无隧道自动恢复及地址变化后的自动改绑。
3. 审核通过或供应商确认放行后，先对用户已授权的测试目标进行一次受控外呼；取得接通结果后继续验收来电、中英两个方向、字幕、挂断和故障清理，记录真实延迟与用量。未实测前不承诺可用、效果或费用。
4. 保留并发改动，将非秘密代码与交接结果提交、推送到开发分支并核对远端；不将草稿 PR 称为已合入 `main`。

## 历史快照：2026-09-22 晚间结论

以下至“历史记录说明”前的内容保留 2026-09-22 当时的代码和验收证据；其中“7 项缺失”“没有真实 Twilio API 验证”及旧下一步已由上方 2026-09-23 结果更新，不代表当前配置状态。

**2026-09-22 晚间完整复核：不能说只差两把 API 密钥，其余全部完成。** 本次恢复 GitHub 连接并获取 `main` 的 `5fabf51`，保留并合并网页版对旧代码的检查记录。完整能力表、7 项缺失配置、实现限制和验收步骤见 [READINESS_REVIEW.md](READINESS_REVIEW.md)。完整交付提交 `457ea22` 已推送到 `codex/local-phone-workbench` 并核对远端，已建立[草稿 PR #2](https://github.com/Richman2020/live-translation-openai-realtime-api/pull/2)，尚未合入 `main`。

本次重跑构建与 54 项离线测试均通过，solo 定向检查通过；全仓库 lint 仍有 1062 个旧 Flex 文件错误，不能称为全量检查通过。本次开始时本机服务与隧道已停止，现已用正式脚本恢复；本机健康检查、鉴权状态和公网访问边界通过。真实 API、号码改绑和电话仍未执行。另确认隧道自动恢复、字幕断线补发、通话中按键菜单、完整延迟/用量记录尚未实现；前端 API 验证超时预算已修正。

当前本机已实现独立中文通话工作台及 solo 后端，默认我方普通话、对方英语；包含主动拨号、来电接听/拒接、挂断/静音、实际字幕事件、可选本机历史、私密设置和手动 API 验证。桌面启动器与安全停止脚本已接到当前仓库，保留旧预览与上游 Flex 实现。

用户已确定本机运行、暂不云端部署，已授权复用现有 Twilio 号码并替换旧语音回调。后续应先补齐私密配置、建立公网入口、准备资源、通过真实 API 验证，再执行号码改绑；不重复询问旧回调是否允许替换。

**已完成代码、54 项离线测试、隔离浏览器 UI、本机桌面启停及真实公网访问边界验证；没有真实 OpenAI/Twilio API 连通、真实电话、双向翻译听感或端到端延迟结果。** 本机服务与 Cloudflare 临时隧道已经运行，`PUBLIC_BASE_URL` 已保存；仍缺少 7 项供应商配置。OpenAI 本机保存确认返回 `not_approved`，未创建或写入密钥；Twilio 内置浏览器登录页空白且 Chrome 连接故障，未取得本机运行凭据。没有改绑号码、开通 Flex 或发起电话。

## 历史本机代码与验证（2026-09-22）

本机目录为 `C:\Users\admin\Documents\ChatGPT\AI电话\live-translation-openai-realtime-api`，开发分支 `codex/local-phone-workbench`；开始开发时已同步 `main` 基线 `1c9eddf548d9783dbb90d8297022f374b742e35f`。当前作为草稿开发分支交付，尚未合入 `main`；最终 commit 和推送以 Git 历史及远端核验为准，不能把尚在本机的修改称为已同步。

| 内容 | 当前结果/证据 | 限制 |
| --- | --- | --- |
| solo 服务与通话状态机 | 新增 `src/solo/`；双侧配对、一次性拨号参数、签名校验、单通话和清理状态 | 真实 Twilio 链路待测 |
| 双向翻译 | 两个独立 Realtime 会话，普通话↔英语，默认 `gpt-realtime-1.5`，真实事件字幕 | 未做真实 OpenAI 会话或语音测试 |
| 中文工作台 | `public/` 复用旧预览视觉；实际状态、字幕、设置、API 验证与可选本机记录 | 隔离 UI 检查不能代替真实电话 |
| 启停与桌面入口 | Windows PowerShell 5.1 下启动、停止及重启实测通过；`D:\桌面文件\AI电话.lnk` 已创建并通过该快捷方式实际启动当前 5050 服务；停止后确认端口释放 | 旧「AI电话（预览）」保留；尚无真实通话中的启停验证 |
| 编译 | Node.js 24.15.0 / npm 11.12.1，`npm run build` 通过 | 只证明构建通过 |
| 离线测试 | 最终 `npm test` 54 项通过、0 失败 | 使用替代连接，未调用真实供应商或拨号 |
| 静态与依赖检查 | `src/solo/` ESLint、独立 scripts TypeScript 检查、锁文件 `npm ci --dry-run` 均通过 | 安装计划检查不等于另一环境的完整安装 |
| Windows 私密配置 | 实际 fixture 验证空临时文件先设置私有 ACL，再写入测试秘密及原子替换 | 测试凭据不是供应商真实运行密钥 |
| 本机启动 | Windows PowerShell 5.1 执行 `Start-AIPhone.ps1 -NoOpen` 成功监听 5050 | 配置缺失仍可启动设置页，通话被拦截 |
| 本机配置检查 | `npm run check:solo` 按预期缺少 7 项供应商配置；模型、本机访问保护与公开地址就绪 | 格式检查不联网 |
| Twilio 准备入口 | `npm run configure:twilio` 在缺少真实配置时按预期 blocked | 尚未真实创建资源或改绑 |
| 浏览器验证 | 已检查中文工作台、设置保存与配置状态；修复视图切换保留页面底部滚动位置；隔离测试页面与测试服务均已关闭 | 隔离浏览器验证，不是供应商接入 |
| 真实公网与鉴权边界 | Cloudflare 临时隧道已启动并保存 `PUBLIC_BASE_URL`；公网 `/api/health` 返回 200，UI/状态接口返回 403；无签名语音 POST 与 WSS 握手均返回 403；本机未授权/非允许来源请求按预期返回 401/403 | 只证明访问限制与无签名拒绝，未验证供应商签名成功或真实媒体流 |
| 接通工具 | 隧道启停、供应商验证、`--prepare` / `--apply` 已实现；隧道停止脚本通过 Windows PowerShell 5.1 解析 | 尚未取得真实供应商验证或号码改绑结果 |

供应商长期密钥留在本机 `.env`；本机访问令牌不打印到日志。工作台/API 只接受本机鉴权访问，公开路由限签名校验的语音入口和最小 `/api/health` 标识探针。挂断失败保留“线路关闭待确认”，允许重试并阻止新拨；停止脚本只有收到 `ok: true` 与 `safeToStop: true` 后才回收自己的后台进程。

## 历史下一步（2026-09-22，当前执行以上方为准）

### 此前同步失败与本轮恢复

此前本机实现提交为 `d9394f4ff12addcbceed3c88fa404edc75b8eb44`，随后推送因 `github.com:443` 连接失败而未完成，GitHub 连接器也曾返回传输错误；当时网页版无法读取该版本。2026-09-22 晚间连接恢复，成功获取远端 `main` 的 `5fabf51` 并保留其并发文档记录。推送时使用本机已配置的系统代理，完整交付提交 `457ea22` 已通过 `git ls-remote` 核对，GitHub 文件接口也已读回本轮复核报告；[草稿 PR #2](https://github.com/Richman2020/live-translation-openai-realtime-api/pull/2) 已创建，尚未合入 `main`。请按 `codex/local-phone-workbench` 读取完整版本，不再把此前网络故障当作当前必然阻塞。

最终补充实测：`Stop-Tunnel.ps1 -Quiet` 成功停止本项目隧道，随后 `Start-Tunnel.ps1` 成功启动新域名、保存到本机配置，新地址的 `/api/health` 返回 200。桌面服务与新隧道在本轮结束时保持运行；临时地址不写入共享文档。

1. 保留并发修改，按共享规则将本轮结果提交推送到草稿开发分支并核对远端；不得将其称为已合入 `main`。
2. 通过受信任的本机设置流程补齐 OpenAI/Twilio 运行凭据；OpenAI 保存确认未批准的问题仍需在其对应流程解决，不能自行绕过。
3. 保持已运行的本机服务和隧道，确认当前公开地址仍有效；补齐设置后查看 Twilio 计划并执行 `configure:twilio -- --prepare`。准备操作创建/更新 Key/App，但不切换号码入站路由。隧道重建后需重新配置地址。
4. 实际 API 检查全部通过后，执行 `configure:twilio -- --apply`；脚本再次验证后，最后替换已获授权号码的 Voice 回调，并清空旧 Voice fallback 和号码 status callback，避免继续流向旧系统。原值及方法写入私密备份，短信路由保持不变，切换后读取远端确认。
5. 使用用户指定测试号码分别验收主动外呼、入站、两语言方向、字幕和故障/挂断清理，记录端到端延迟与用量。没有实测前不承诺亚秒、效果或费用。

## 历史记录说明

以下保留先前 Work/网页环境的代码、账户检查与交接历史。其“6 项测试”“尚未实现单人版”“待选择架构”等描述是当时状态，已由上方本机实现和当前下一步更新；历史账户登录或 GitHub 写入也不等于本机取得供应商运行凭据。请按日期、环境与证据范围读取，不用历史阻塞覆盖当前结论。
### 网页版在本机代码同步前的历史复核

以下两段来自远端 `5fabf51`，只描述当时尚未包含 solo 实现的 Work 环境；本次完整保留，不能用于否定上方本机新增代码。

2026-09-22 当时复核：构建和 6 项离线测试再次通过；配置检查仍有 7 项占位值，当前 Work 环境的 5050 端口没有服务监听。共享代码仍缺少中文使用者直接拨号的界面与通话流程，整体尚未跑通，不能把仓库同步成功称为电话系统已接通。

用户报告电脑端 Codex 已安装一个桌面快捷方式。当前 Work 环境不能直接读取该电脑的本地文件或会话；该桌面版本的实现、配置和测试结果尚未在这里核验。不能将这里的缺少配置推断为用户电脑也缺少配置。

## 本文件对应的代码快照

- 起点：上游原版提交 `ce3c01a12bd9df2c56adf54073cce2d6f861f6b4`。
- 本批交接分支：`codex/shared-handoff-20260921`；是否已推送或合入 `main` 以 GitHub 实际提交为准。
- 本记录描述与本文件一同交接的代码快照及此前验证结果，不代表所有运行环境都已安装这一版本。
- 同步状态以 GitHub 上实际存在的分支与 commit 为准；是否合入 `main` 应核对远端分支历史。执行同步的人必须核对远端，并在交接消息中给出分支与 commit，不能只凭本地记录声称推送成功。
- 之前提供过安装修复补丁。后续接手应以仓库中的代码为准，不要重复应用已经包含在当前版本中的旧补丁。

## 已完成的代码修改

| 内容 | 文件或入口 | 状态说明 |
| --- | --- | --- |
| 依赖与构建修复 | `package.json`、`package-lock.json`、三个路由文件 | 补齐锁文件缺失的 esbuild 可选平台依赖；保持已有依赖版本；修复路由类型和跨平台清理构建目录 |
| 本地初始化 | `npm run setup:local` | 仅在 `.env` 不存在时从模板创建，不覆盖已有配置 |
| 配置检查 | `npm run check:config` | 检查变量是否缺失、仍是占位符或格式错误，不输出密钥值、不联网；开发和正式启动前先检查 |
| Realtime GA 适配 | `src/services/realtime.ts`、`AudioInterceptor.ts`、`src/config.ts` | 模型可配置，默认 `gpt-realtime-1.5`；使用 GA 会话字段与音频事件，保持 Twilio μ-law 音频格式 |
| 音频生命周期处理 | `AudioInterceptor.ts`、`StreamSocket.ts` | 各连接等待会话配置确认；缓冲限制为约 2 秒；保留 Flex 初始提示音抑制；处理异常事件与挂断后的发送边界 |
| 离线测试 | `tests/realtime.test.ts` | 使用内存模拟连接，不调用付费 API |
| 可持续交接 | `AGENTS.md`、`PROJECT_BRIEF.md`、本文件、`LOCAL_SETUP.md`、`README.md` | 保存需求、规则、安装步骤和真实进度，供其他会话与电脑端 Codex 读取 |

## 已有验证证据

以下是本次安装工作在当前 Work 环境取得的结果，不代表用户电脑的验证结果。环境为 Node.js 24.19.0、npm 11.9.0。

| 检查 | 结果 | 能说明什么 |
| --- | --- | --- |
| 依赖安装 | 通过 | 当前环境可安装依赖 |
| `npm ci --ignore-scripts --no-audit --no-fund --dry-run` | 通过 | 修复后的锁文件通过安装计划检查；不是另一台电脑上的实际安装记录 |
| `npm run build` | 通过 | TypeScript 可编译 |
| `npm test` | 6 项通过 | 模拟验证 GA 配置、会话就绪缓冲、提示音处理、双向音频路由、缓冲上限与异常/挂断边界 |
| `git diff --check` | 安装修复阶段通过 | 未发现该阶段的空白格式问题；后续修改仍应检查 |
| `npm run check:config` | 按预期未通过 | 当前 Work 环境中的 OpenAI/Twilio/ngrok 必需配置仍为占位符，启动检查可以拦截未完成配置 |

## 尚未验证与待完成

- 当前 Work 环境没有填入完整 OpenAI API 密钥。创建密钥不等于这个运行环境已经接入密钥；不应要求把密钥提交到仓库。
- 本环境的 Twilio 号码、认证、Flex/TaskRouter/Studio 与 ngrok 配置未完成，真实服务未启动。按 `AGENTS.md` 的运行边界，先完成这些配置再启动通话服务。
- 没有运行真实 OpenAI Realtime 会话，模型权限、计费、翻译质量与连接稳定性均需实测。
- 没有进行真实双向电话、实际听感、连续通话、端到端延迟或费用测试；不能宣称“整个链路已经打通”。
- 独立桌面界面、中文使用者主动外呼、字幕与 WhatsApp 接入没有在本仓库取得实现/验收证据。先核对电脑端 Codex 的已有工作，再补充开发。

## 下一个执行者的操作顺序

1. 获取远端最新代码，先读交接三份文件并检查分支和本地改动。保留电脑端已有工作，将其提交到适当分支，并记录桌面快捷方式实际启动的命令/程序、已实现功能与测试结果；不要上传秘密配置。
2. 安全整合两边改动，确认安装脚本与 Realtime 修改已包含在将要运行的版本中。在目标电脑执行安装、构建和离线测试，记录该电脑的实际结果。
3. 在用户电脑核对已有实现，并确定原版 Flex 流程还是单人通话模式。选择 Flex 时按 `LOCAL_SETUP.md` 配置；选择单人模式时先实现其替代流程及对应配置检查，不使用占位 Flex 值绕过启动检查。密钥和公开 HTTPS/WSS 地址均在实际运行环境配置。
4. 验证选定流程的两个语言方向，记录真实延迟和失败情况；分别验证主动外呼、入站接听以及挂断行为，不把某一条流程成功当成所有场景已接通。
5. 更新本文件与需求文档，提交并推送，向用户报告远端已核验的分支、commit、已验证能力和剩余工作。推送失败时明确报告，不把本地改动视为共享完成。

## 后续记录格式

每次增加或更新记录至少包括：日期、执行环境、实际修改、运行的验证与结果、未验证内容、下一步，以及可核验的分支/提交或 PR。提交本身的完整 SHA 由 Git 历史和最终交接消息提供，不需要为把一个提交自己的 SHA 写入该提交而反复提交。

## 2026-09-21：Twilio 接入检查

以下按发生顺序保留历史检查；后续新增记录可以解除当时的阻塞，当前状态以上方“当前结论”和最后一节为准。

- 已验证 Twilio 开发工具包处于已安装、已启用状态；当前会话没有提供直接读取或修改 Twilio 账户资源的连接器工具。工具包启用不等于账户连接已经验证。
- 当前 Work 环境未安装 Twilio CLI，没有 CLI profile，项目必需的 Twilio/OpenAI/ngrok 配置仍为占位符，应用端口未监听。这些检查仅针对本 Work 环境，不能推断用户电脑端 Codex 的配置状态。
- Twilio 账户连接仍待完成验证；本次未读取或修改账户中的号码、Flex、Studio 或 TaskRouter 资源，未启动真实通话。
- 下一步：完成账户授权并验证连接，核对现有号码及其当前用途，确认目标运行环境可用的公网 HTTPS/WSS 地址，再配置并验证原示例的 Studio、号码回调和 Flex/TaskRouter 流程。

## 2026-09-22：Twilio 账户只读核验

- 用户授权后，主线程通过 Google 登录成功进入 Twilio 新控制台，并从实时页面确认当前账户为 Active、有可用余额。此项验证通过浏览器完成，不代表开发工具包已向应用提供账户 API 连接或运行凭据。
- 当前选定账户的号码库存显示一个美国本地号码，Voice 已启用；Messaging 未启用，页面显示待 A2P。语音接入与短信注册应分别判断。
- 现有号码的 Voice Webhook 和通话状态回调已经连接外部业务系统，均使用 POST，区域为 US1。尚未确认原系统用途及是否允许改绑，因此保留原配置，不直接覆盖。
- 美国和加拿大的低风险外呼目的地开关已开启；当前 CPS 为 1。这些账户设置不代表翻译通话已经接通。
- 本 Work 环境重新运行 `npm run check:config`，退出码为 1；`OPENAI_API_KEY`、`TWILIO_ACCOUNT_SID`、`TWILIO_AUTH_TOKEN`、`TWILIO_CALLER_NUMBER`、`TWILIO_FLEX_NUMBER`、`TWILIO_FLEX_WORKFLOW_SID`、`NGROK_DOMAIN` 全部仍为 `PLACEHOLDER`。此前简易非空检测把部分占位配置判为已配置，属于假阳性；以后以项目配置检查与实际连接验证为准。
- Twilio CLI/profile 和 ngrok CLI 在本 Work 环境仍未配置，应用端口未监听。以上只描述本环境，不能据此推断用户电脑端 Codex 的配置。
- 已核验当前账户 US1 的 Studio Flows 列表为空。Flex Overview 显示 Get access to Flex：在当前账户启用 Flex 采用 User + Usage 计费且没有试用；另建 Flex 专用账户提供试用，但页面明确不能使用当前账户的号码或其他资源。没有选择方案、接受条款或开通 Flex。TaskRouter 尚未单独验证。
- 本次没有购买号码、更改回调或发起测试电话；真实双向翻译、通话延迟及费用仍未实测。
- 已核验 GitHub 原共享代码位于 `main` 提交 `f1021446`。本次新增记录是否已提交、推送或合入，以后续执行者核对的远端实际提交为准；不能把本地追加视为同步完成。
- 下一步：确认现有外部业务系统的用途及号码安排，明确目标运行环境；私下补齐真实密钥、号码与工作流配置及可用的公网 HTTPS/WSS 地址，核对 Flex/Studio/TaskRouter 资源，完成回调配置后再启动并实测。

## 2026-09-22：用户确认替换旧系统，准备 OpenAI 接入

- 用户已明确批准复用当前号码、替换旧系统语音接入，并要求接入 OpenAI 完成双向通话闭环。前一节“是否允许改绑”的阻塞已解除；尚未实际修改号码回调。
- 已重新读取 GitHub `main` 的共享规则、需求和进度文件，未发现新的桌面端或主动外呼实现。当前仓库仍是依赖 Flex 的后端示例，不能只填 OpenAI 密钥就声称满足桌面主动拨号需求。
- 已只读核对现有部署项目列表，未找到明确对应本翻译仓库的服务；未复用其他业务的服务或凭据，未创建收费部署。
- 本 Work 环境未获得可用的 OpenAI/Twilio 运行密钥。已检查可用技能，未找到 Codex 本地安全写入 OpenAI 密钥的专用安装流程；未自行生成、提取或复制密钥，也未把网页登录状态当成应用认证。
- 待确认路线：可以继续配置原版 Flex 流程，但当前账户尚未开通 Flex；另一候选是单人浏览器电话模式，新增 Voice SDK 接入和主动拨号流程以去除 Flex 依赖。后者目前仅完成只读代码评估，并未实现或验收。
- 后续顺序：明确目标运行环境及费用范围，在该环境安全配置密钥并验证 API 连接；完成选定通话流程与公网 HTTPS/WSS；服务就绪后切换号码；使用用户确认的测试号码进行中英双向通话和端到端延迟测试。
- 本次没有新增付费资源、购买号码、删除旧系统或拨打电话，也没有完成 API 实连或双向翻译测试。
- 同步状态：GitHub 连接器此前写入返回权限错误，浏览器文件编辑页仍未加载出编辑器。本节与需求更新目前只在本地，未推送成功；后续应先恢复正常仓库写入权限/界面，再提交并核验远端，不把本地记录视为共享完成。

## 2026-09-22：确定本地路线并排查 GitHub 同步

- 用户明确现阶段不部署云端服务，后续继续在其电脑端 Codex 开发、运行；不再把选择 Railway 或新增云端预算当成推进前提。
- GitHub 只读接口返回该仓库当前账户拥有 admin/push 权限，远端共享文件可正常读取。账户的仓库权限与连接器应用的授权范围是两回事；此前 `403 Resource not accessible by integration` 指向调用凭据权限不足，不能直接归因为用户没有登录网页。
- 需要将本地已有的账户核验、旧号码复用授权、本次本地运行决定同步到共享仓库。同步结果以后续核验的远端 commit 为准。
- 单人版是去除 Flex 依赖的候选架构，不是新的电话供应商或已完成的软件；仍使用 Twilio 电话线路与 OpenAI 翻译。原示例的 Flex 坐席、Studio 和 TaskRouter 配置不能直接套用到尚未实现的单人模式。
- 本次不应在云端环境配置一套与用户电脑无关的密钥后声称电脑端已接通。实际密钥、HTTPS/WSS 隧道与 API 实连验证应在用户电脑执行；这里可以准备代码、配置模板及交接记录。
- 本轮对 `PROJECT_BRIEF.md` 做了一次真实的连接器写入尝试，仍返回 `403 Resource not accessible by integration`，没有成功提交。浏览器的新窗口确认当前登录身份为仓库所有者，随后编辑页与应用设置页出现加载超时；这与登录失败应区分。
- 当时查阅的 OpenAI 官方说明描述普通 ChatGPT GitHub 应用的读取能力，并建议通过 Codex 编辑、推送。这个说明与此前的 403 不应被泛化为本会话永久不能写入；连接权限改变后，必须以实际写入和远端核验为准。仅重新登录网页或重置密码也不保证解决应用权限问题。来源：https://help.openai.com/en/articles/11145903-connecting-github-to-chatgpt 。
- GitHub 插件为已安装、已启用状态；本 Work 环境没有已安装的 GitHub CLI。没有删除或重装现有连接，没有扩大应用授权，也没有更改账户密码。
- `git diff --check` 通过；重新运行 `npm run check:config` 仍因 7 项占位变量退出 1，未调用真实 OpenAI/Twilio API。本轮为文档交接修改，未实现新的通话流程。
- 已准备 [LOCAL_CODEX_HANDOFF.md](LOCAL_CODEX_HANDOFF.md)，供用户电脑上的 Codex 按现有 Git 权限合并文档、提交推送，并在目标电脑完成后续配置。补丁准备完成不代表已经同步 GitHub；只有远端可读取的新 commit 才能作为同步成功证据。

## 2026-09-22：权限调整后恢复仓库写入

- 用户报告已在 Codex 调整仓库权限，并明确要求重试同步。主线程重新读取 `main`、共享规则和两份交接文档；当时 `main` 仍为 `f1021446c3ee760a3c00d9683ec68ff72684a639`，没有发现需要合并的新增远端提交。
- 对 `PROJECT_BRIEF.md` 的真实写入已成功，GitHub 返回提交 `d1ebefe2cb15e2ecaf05a11a2b728aa6726d4238` 和内容 blob `c62afa6ae82b7e3c81870eb82450d4c917e6074f`。这证明当前连接器已能修改本仓库，取代此前写入受阻的状态；不是只确认插件已安装或账户已登录。
- 本批补齐 `PROGRESS.md`、`PROJECT_BRIEF.md` 和 `LOCAL_CODEX_HANDOFF.md`：记录账户核验、旧号码复用授权、本地运行决定、密钥尚未配置以及电脑端下一步；修正历史权限结论与强制先开通 Flex 的旧计划表述。
- 此次为文档同步，没有重新上传已在 `main` 的安装及 Realtime 代码修改，没有写入 `.env` 或其他秘密，也没有变更 Twilio 回调、开通 Flex、部署云服务或拨打电话。
- 文档已做人工核对、敏感信息检查和 `git diff --check`。最终分支及完整提交 SHA 应由 Git 历史与同步后的远端核验消息提供；读取到本批最新文档后，不要再重复应用旧交接补丁。
- OpenAI/Twilio 密钥配置、真实 API 连通、双向电话翻译与延迟测试仍未完成。下一步由用户电脑端 Codex 获取最新仓库，在本机核对已有工作和私密配置，继续完成选定流程。

## 2026-09-22：本机新增 solo 工作台与接通工具

- 当前仓库已在 `codex/local-phone-workbench` 实现独立单人桌面通话、字幕、私密设置和安全启停，替代前述历史记录中的“候选、尚未开发”状态；上游 Flex 和旧预览保留。
- 最终本机编译、54 项离线测试、solo ESLint、scripts TypeScript、锁文件安装计划检查均通过；Windows 私密临时文件 ACL fixture 验证通过。桌面快捷方式实际启动、停止/重启与真实公网访问边界均已验证，细节见顶部表格；隔离 UI 测试页面及服务已关闭。
- 新增隧道启停、供应商验证及 Twilio 资源准备/号码改绑工具。真实隧道已运行、公开地址已保存；已有号码改绑授权继续有效，实际切换仍留到 API 检查成功之后。改绑会清空旧 Voice fallback/status callback，已扩充私密备份，短信路由不变。
- OpenAI 本机保存确认返回 `not_approved`，Twilio 登录/浏览器连接未完成，因此尚未配置真实运行凭据、验证真实 API、改绑号码或拨打电话。下一步以本文件顶部“当前下一步”为准，不再重复执行历史中的架构选择或旧补丁步骤。
## 2026-09-22：整条链路复核（Work 旧版历史记录）

以下为远端 `5fabf51` 保留的并发检查记录，检查对象未包含本机 solo 工作台；当前剩余工作以上方本机复核为准。

- 用户要求检查整个项目是否跑通及还缺什么。检查起点为 GitHub `main` 提交 `1c9eddf548d9783dbb90d8297022f374b742e35f`；当时未发现更新的远端提交。按 Git blob 哈希逐一比对远端全部 44 个文件与当前工作区，内容全部一致。当前本地 Git HEAD 较旧，包含已在远端合并的暂存修改，因此没有重置工作区或将旧分支强推到远端。
- 本次验证环境为 ChatGPT Work（Node.js 24.19.0、npm 11.9.0），不是用户电脑。重新运行 `npm run build` 成功，`npm test` 的 6 项测试全部通过；测试使用内存模拟连接，不调用 OpenAI/Twilio。
- `npm run check:config` 退出 1：OpenAI API key、Twilio Account SID/Auth Token、来电号码、Flex 号码/Workflow 与公网域名共 7 项仍为占位值。只记录状态，没有打印秘密。当前环境的 5050 端口未监听，ngrok CLI 不可用；未启动服务，也未调用真实 API。
- 代码复核：`src/routes/index.ts` 只注册健康检查及原版 Flex 路由；`outbound-call.ts` 把电话排入 Flex Workflow，`intercept.ts` 的电话创建目标是固定 Flex 号码。没有给中文使用者输入客户号码、操作浏览器麦克风及主动拨号的独立界面，也没有桌面应用封装。
- 翻译启动仍依赖 `flex-reservation-accepted.ts` 的坐席接受事件，配置仍要求 Flex。此前账户页面检查为 Flex 未开通、Studio Flow 为空，本轮没有重新读取或修改账户页面；这些资源不是本轮新完成的配置。
- `realtime.ts` 启用音频转写，但 `AudioInterceptor.ts` 未将转写发送到字幕界面；不能将启用转写说成已实现字幕。提示词仍要求一轮话结束后翻译，现有计时只覆盖语音结束事件至生成的首个音频包，尚无听众实际听到声音的端到端延迟数据。
- 结论：开发基础可编译，模拟音频路由通过测试；真实 API、电话接通、中英双向翻译及用户要求的低延迟仍没有验收证据。用户电脑上的快捷方式、私密配置和未推送代码仍需在电脑端核对，不能据 Work 环境状态推断其电脑也未配置。
- 下一步：电脑端 Codex 先同步实际桌面版本，再补齐用户直接通话的流程（单人模式仍为建议路线，尚未选择或实现）；在目标电脑配置真实凭据及可达的 HTTPS/WSS，先验证 OpenAI Realtime 与 Twilio API，再在服务就绪后接入已获授权的号码。最后使用用户明确指定的测试接听号码验证双向翻译、挂断、连续对话与两个方向的实际延迟。
- 本轮仅同步检查记录，没有开通 Flex、部署云服务、购买号码、修改回调或拨打电话。提交与远端核验结果以 Git 历史和本轮交接消息为准。
