# 固定稿 V1：合成音频转写 A/B（2026-09-26）

本轮比较 `whisper-1` 与 `gpt-4o-transcribe`，只测输入音频转写（ASR）。两者均保留了 12 行合成稿的关键语义；本次 GPT-4o 的最终转写等待时间较短，可作为下一轮真人测试的候选。**这不是现实口音准确率、真实电话翻译质量或耳听端到端延迟验收，也不代表已经切换部署模型。**

## 方法与边界

- 固定原文取自 [PHONE_TEST_V1.md](PHONE_TEST_V1.md) V1.0 的 01–12 行，未向 API 提供预期答案。
- 本机 Windows SAPI 生成中文 Huihui、英文 Zira 语音，8000 Hz、PCM16、单声道，再转为 G.711 μ-law（PCMU）。共 25.668 秒、205,344 字节原始 PCMU；音频均非静音。
- 每段加 300 ms 前置静音和 1300 ms 尾部静音，按 20 ms／160 字节实时节奏回放。相同帧背靠背送给两个模型；逐句音频 SHA-256 和实际发送字节数一致。每模型含补静音共发送 358,944 字节。
- 使用现有 `gpt-realtime-1.5` Realtime 音频输入会话，两个模型各分中文、英文两条连接。唯一比较变量为转写模型；中文 `zh`、英文 `en`，无转写 prompt。
- 严格回读确认：`server_vad`、阈值 0.5、前缀 300 ms、静默 500 ms，`create_response:false`、`interrupt_response:false`。服务端降噪请求保持未设置，回读为 `null`。没有改变 VAD、阈值或默认模型。
- 每模型 12 行各提交一次，共 24 次；没有重试挑选结果、没有 `response.create`、没有生成翻译语音、没有 Twilio 外呼。硬截止 240 秒。
- 实际 UTC 06:36:56.345–06:37:46.242，总运行 49.897 秒；没有 API 错误、等待超时或归属不明的转写事件。4 条连接均以 1000 正常关闭。

## 逐行复核

以下全部是公开测试稿的合成内容，不包含真人通话字幕。按语义复核，标点、简繁体及 `OK`／`Okay` 的等价形式不单独判错；没有声称逐字完全相同或计算通用识别准确率。

| 编号 | 合成原文 | Whisper 实际转写 | GPT-4o 实际转写 | 关键语义 |
| --- | --- | --- | --- | --- |
| 01 | 你好。 | 你好 | 你好。 | 两者保留 |
| 02 | Hello. | Hello | Hello. | 两者保留 |
| 03 | 你叫什么名字？ | 你叫什么名字? | 你叫什麼名字? | 两者保留问句 |
| 04 | My name is Tom. | My name is Tom. | My name is Tom. | 两者保留姓名、第一人称 |
| 05 | 你今天有空吗？ | 你今天有空吗? | 你今天有空嗎? | 两者保留今天、问句 |
| 06 | No. I am free tomorrow. | No, I am free tomorrow. | No, I am free tomorrow. | 两者保留否定、明天 |
| 07 | 我们明天下午三点见。 | 我們明天下午三點見。 | 我们明天下午三点见。 | 两者保留明天、下午、三点 |
| 08 | OK. Do you want water? | Okay. / Do you want water? | Okay. / Do you want water? | 两者保留，均分为 2 个回合 |
| 09 | 我要两杯水。 | 我要两杯水。 | 我要两杯水。 | 两者保留两杯 |
| 10 | Do you want coffee? | Do you want coffee? | Do you want coffee? | 两者保留问句 |
| 11 | 我不要咖啡。 | 我不要咖啡。 | 我不要咖啡。 | 两者保留否定 |
| 12 | Thank you. Goodbye. | Thank you. / Goodbye | Thank you. / Goodbye | 两者保留，均分为 2 个回合 |

表内 `/` 只标记独立回合边界，不是模型说出的字符。08、12 行中的短暂停顿触发了分段，因此每模型产生 14 个最终转写回合。字幕条数超过稿件行数，本身不能证明重复或凭空说话。

## 计时结果

所有时间都由同一本机进程的单调时钟记录。下表不含翻译生成、Twilio 传输和接收者播放；小样本单轮统计不能作为稳定延迟承诺。

| 指标 | Whisper | GPT-4o Transcribe |
| --- | ---: | ---: |
| 完成输入行数 | 12/12 | 12/12 |
| 最终转写回合数 | 14 | 14 |
| 收到 `speech_stopped` → 收到该回合 ASR final，中位数（n=14） | 1068 ms | 486 ms |
| 同指标最小／最大值 | 342／2004 ms | 424／956 ms |
| 源音频最后一块发送 → 收到该行最后 ASR final，中位数（n=12） | 1036 ms | 575 ms |
| 同指标最小／最大值 | 421／2058 ms | 457／1073 ms |

`speech_stopped` 接收时间是服务端停说事件到达本机的时刻，不是用户实际停止讲话的时刻。源 WAV 自带尾静默，因此“源音频最后一块”也不等于真实最后一个音节。尾部额外 1300 ms 静音的发送完成时刻另存于原始证据，未冒充说完时间。对分成多个回合的行，整行指标使用最后一个 final；首个短句可能在整行发送完前就完成识别，不能把该负差值当作完整一行的延迟。

## 必须保留的历史回归与下一步

- 更早的 4 句合成对照中，`gpt-4o-transcribe` 曾将“你好，请问你今天几点下班？”识别为“你好，请问你天天几点下班?”，随后译成 `every day`；同一历史素材的 Whisper 轮次保留了“今天”。本轮较简单的 12 行通过不能撤销该失败，也不能说明候选模型对所有中文更准。
- 本轮没有真人口音、实际麦克风、声学回录、背景噪声、丢包或 Twilio 链路。只有两套固定 SAPI 声音，没有验证真实静默幻觉或自由交谈。
- 下一轮可显式选用 `gpt-4o-transcribe` 作为单变量真人候选；继续保留原 VAD、阈值、停顿及固定稿，并记录逐句 ASR、生成首音频和实际耳听结果。项目默认值与本机显式配置应分开表述，运行值以实际配置及供应商回读为准。
- 原始脚本、素材、哈希、请求回读、逐回合时间和汇总保留于本机 Git 忽略目录 `.runtime/quality-check/asr-v1-ab/`，主要文件为 `manifest.json`、`run-ab.ts`、`result.json`、`summary.json`。历史结果仍在 `.runtime/quality-check/gpt-4o-transcribe-result.json` 与 `grounded-text-result.json`；这些本机证据不随仓库提交分发。
