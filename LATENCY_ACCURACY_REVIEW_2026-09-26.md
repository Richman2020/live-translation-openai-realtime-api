# 识别与延迟：真实电话后的分析

日期：2026-09-26。证据范围为 09:58:51.849–10:00:18.058 UTC 的用户测试。此次没有修改运行代码、模型、私密配置或供应商资源，没有重启或另行拨号。

## 用户目标与测量

正常音量、正常语速下中英双向准确传达，同时减少停说到实际听到译音的等待。用户本轮仍报告约 3 秒及“声音短”；后者尚需区分截断、音量小或切句碎片，不能直接归因模型。

运行选择为 `gpt-4o-transcribe` → `gpt-realtime-1.5`。源代码 `src/solo/translation-bridge.ts` 使用 500 ms 静音判断，等待最终转写，再以 JSON 原文封装请求独立翻译语音；首音频到达即转发。项目默认转写仍为 `whisper-1`。

| 方向 | 段数 | 转写等待中位 ms | 排队中位 ms | 生成首音频中位 ms | 总耗时中位 ms | 总耗时最大 ms |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 电脑中文 → 手机英文 | 8 | 406 | 0 | 888 | 1273 | 1702 |
| 手机英文 → 电脑中文 | 12 | 456 | 0 | 798 | 1394 | 1545 |

计时从本机收到 `speech_stopped` 到收到第一段生成音频。20 段都有完整数据，单条分段相加等于其总耗时；各列中位数不能直接相加。电脑方向仅一次排队 213 ms，手机方向均无排队，两条 OpenAI 连接未断开或重连。

这些数值不包含此前形成停说判断的约 500 ms 配置等待，也不覆盖完整传输和终端播放。不能以用户估计的 3 秒减去表格数值，称差值就是网络延迟。单轮数据不证明长期表现。

电脑 → 手机 8 段生成/写入共 94800 字节，7 段有播放完成确认，1 段 7200 字节在结束时未确认；手机 → 电脑 12 段共 120400 字节，均有确认。确认不证明耳听完整、音量合适或译意正确。

页面原文与固定稿存在否定句、名词及碎片差异，译文会跟随错误原文。没有保存真实源音频，仍不能在实际说法、采音、切句、识别模型之间确定唯一原因，也不能从额外字幕数量直接断言幻觉。原始数值保存在忽略的 `.runtime/test-runs/20260926-0958-1000-runtime-summary.json`，共享文档不含真实完整字幕、号码或账户资料。

## 准确度的处理

系统已有识别模型，增加一个插件不会自动修复输入。下一轮应固定实际耳麦、隔离外放，以正常音量同素材比较；参与者知情后保留可核对的源音频和播放证据，定位低音量、削波、丢音及句尾截断，再比较模型。

浏览器请求过回声消除、降噪、自动增益，不证明效果；服务器当前没有额外输入降噪或转写提示。不要同时盲调多项参数。可评估真实业务术语提示，但不能把整份测试答案写入提示词。继续使用 [固定稿 V1](PHONE_TEST_V1.md)，重点保留否定、数量、时间和名字。

Twilio Media Streams 为单声道 8 kHz μ-law，存在电话带宽限制；本轮未证明它就是误识别根因，升采样也不会恢复丢失的细节。[Twilio 格式及播放事件](https://www.twilio.com/docs/voice/media-streams/websocket-messages)

## 延迟的处理与候选

当前必须依次等待停说、最终文字和翻译语音。单独优化识别阶段的收益是本轮约 0.4 秒量级，不能据此承诺耳听从 3 秒降到 1 秒。静音阈值由 500 改为 300 ms 只减少配置上的 200 ms，可能拆散慢速英语。语义 VAD 可改善切句，但不保证更快。[OpenAI VAD](https://developers.openai.com/api/docs/guides/realtime-vad)

| 路线 | 价值 | 待验证问题 |
| --- | --- | --- |
| 现有最终 ASR → 翻译语音 | 输入可追溯，保留为对照与回退 | 串行等待；再叠加纠错模型可能更慢 |
| 专用实时语音翻译 | 原声仍在输入时持续产生译音和字幕 | 账户权限、语言配置、自然口音、否定和长句完整性 |
| 独立流式 ASR → 翻译 → TTS | 各环节可独立选择 | 等整句仍慢；提前播出不稳定识别无法撤回 |

官方已提供 `gpt-realtime-translate` 与专用 `/v1/realtime/translations`，建议作为首选独立候选。它不是当前通用助手模型的配置替换：WebSocket 使用 24 kHz PCM16，需要与 PCMU 双向转换，并适配每方向会话、字幕、音频事件与结束排空。本轮没有调用该 API，账户可用性和中英表现未验证。[官方模型](https://developers.openai.com/api/docs/models/gpt-realtime-translate)、[实时翻译指南](https://developers.openai.com/api/docs/guides/realtime-translation)

新的独立流式识别 `gpt-live-transcribe` 也有延迟档位，但不支持直接沿用现有服务器 VAD，需要另外适配；低延迟与上下文有取舍。[实时转写指南](https://developers.openai.com/api/docs/guides/realtime-transcription)

本机中转、代理、公网隧道和终端播放仍需分别计时，没有证据说明迁移服务器具体能节省多少。本轮保留本机运行决定。

## 执行与验收顺序

1. 固定设备和 V1 稿，补真实输入与实际播放证据，明确“声音短”的具体表现。
2. 独立验证专用翻译接口权限、语言和编解码，再适配字幕、排空、重连；失败明确报告，不静默切换当前模型。
3. 同素材对照后开展两人自然音量电话，测实际停说到首个有意义译音，检查语义、句尾、额外话语、静默、长句。
4. 建议第一阶段目标为典型短句中位等待降至 2 秒以内且无漏译/额外内容；这是建议，非已确认硬阈值或已有结果。保留最慢句，不能只报平均。
5. 候选通过后才受控切换，保留回退并复验拨号、双向音频、字幕、挂断和连续多通，避免音质优化破坏电话链路。

本次仅文档更新，进行差异检查，不重跑与文档无关的源码测试。分析完成不等于新方案实现或电话质量验收。
